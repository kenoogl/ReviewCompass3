# 契約014正準列定義の未記載2点の注記＋挙動固定（候補4） 作業票 v1（範囲固定・軽量）

- 作成日：2026-08-18
- 指示者：利用者（Human）。選択文言「その後、候補4対応」（2026-08-18 chat）
- 作成者：Claude
- 種別：範囲固定文書（軽量作業票）。**実装変更ゼロ**（文書注記＋現挙動を固定する試験2本の
  追加のみ）。製品コードを変更しないため正式再利用検索は適用外
- 固定入力：現挙動の機械確認record
  `records/development/2026-08-18-contract-014-canonical-sequence-gaps-observation-v1.md`
- 対象候補：`IC-CONTRACT-014-CANONICAL-SEQUENCE-GAPS-001`

## 1. 目的

契約014の正準列定義（候補v3 §7.1）に書かれていない2つの場合（入力終端・Codex 2形式との関係）
について、実挙動が安全側であることを機械確認できたので（観測record §1・全10 probe）、
(a) 契約本文への注記と (b) 挙動を固定する試験の追加で欠落を閉じる。**契約v4の改定はしない**
（挙動・境界の変更がないため。観測record §3）。

## 2. 正本範囲（成果物）

1. **契約候補v3への注記**：`records/task-contract/2026-08-17-session-log-prefix-interpretation-candidate-v3.md`
   の末尾へ注記節を追加。**原文は1文字も変えない**（受入済み契約の記録を保つ。候補2で裁定
   された注記形式と同じ原則）。内容＝未記載2点の実挙動（終端→非対応・前置スキップは
   Codex 2形式判定にも効く・偽装前置は誤判定されない）と観測recordへの参照。
2. **敵対試験2本の追加**：`tests/test_session_log_prefix_interpretation.py`へ——
   - 前置のみで入力終端（判定可能record不在）→ 非対応（`None`）
   - 前置1件 → Codex 2形式 → `codex_exec_json`（前置スキップがCodex 2形式判定にも効く）
   いずれも**現挙動を固定する試験**（実装変更なしのため追加時点で通る。挙動の退行を検出する
   目的）。

## 3. 範囲外

- `tools/session_logs/`配下の実装変更（現挙動は安全側で変更不要——観測record §3）。
- 契約014のv4改定・再受入。
- RQ2実験のケース材料`docs/evaluation/rq2-cases/`の更新（封緘済み）。

## 4. 受入条件

1. 契約候補v3の**原文が無変更**（`git diff`で追加のみを機械確認）。
2. 追加試験2本を含むsession_logs系全域の全通過（単独終了コード0）。
3. RQ2材料無変更・正解表v2のdigest表全件一致。
4. `git diff --check`・意味単位commit・`work_unit_transition`合格。

## 5. 実施順序

1. 契約候補v3へ注記追加 → 原文無変更を機械確認。
2. 試験2本追加 → 全域実行。
3. 受入条件3の機械確認 → commit → 移行検証 → 完了報告。
