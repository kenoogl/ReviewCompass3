# レビュー対象分類・工程分離の機械接続 軽量作業票 v2

- 作業票ID：`RC3-REVIEW-TARGET-PROCESS-CONNECTION-BOOTSTRAP-2026-08-13-V2`
- 作成日：2026-08-13
- 基準作業票：`docs/development/2026-08-13-review-target-process-connection-bootstrap-work-ticket-v1.md`
- 基準作業票SHA-256：`486a70f1d02833cf14ac571001bb94476c8ac716d8555d0e619c85967ca62f04`
- 開始前レビュー記録：`records/development/2026-08-13-review-target-process-connection-bootstrap-start-review-v1.md`
- 状態：`correction_review_pending`

## 1. 修正範囲

【実測】開始前レビューは、v1 §4の三案がすべて新しい実装を要し、`AGENTS.md` §4が必須とする
「既存機能・設定だけで済ませる最小案」を欠くと判定した。他の範囲、入力・出力、警告と不合格、
利用者承認、RED／GREEN、完了条件には止める指摘がなかった。

【判断】v1 §4の三案比較だけを次の内容へ置き換える。v1のその他の節は変更しない。

## 2. v1 §4を置き換える三つの実装案と選択

| 案 | 方法 | 単純さ | 処理時間・メモリ | 頑健さ | 変更範囲・保守 | 戻しやすさ | 判定 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A | 現行コードと設定を変更せず、人がGit差分を対象別に分け、既存方針を各群へ個別適用して結果をまとめる | 実装は最小だが、毎回の手順は複雑 | path数に比例、保持量は少量 | 分類漏れ、余分、差分との不一致を機械検出できず、今回の接続漏れを再発し得る | 実装変更0、反復手作業の保守負担が大きい | 最も高い | 不採用 |
| B | `docs/`、`tests/`、拡張子などから全対象を自動分類する | 単純 | path数に比例、少量 | 検査コードと製品コードの意味差をpathだけでは確定できず、誤分類を隠す | 小さいが判定規則の保守が増える | 高い | 不採用 |
| C | 全変更pathと対象種別を対応付けた一つの構造化入力を受け、Git差分と照合して対象別に既存方針評価を呼ぶ | 中程度 | path数に比例。入力と出力を保持するだけ | 漏れ、余分、未知種別、混在を明示でき、意味分類をpath推測で置換しない | 既存4 path内。新しい台帳なし | 入力追加と群出力を戻せばよい | 採用候補 |

【判断】案Cを採用候補とする。案Aは既存機能だけで済む最小案だが、対象分類とGit差分の対応を
人の反復手作業へ戻すため、承認済みの機械接続と今回の再発防止を満たさない。案Bは操縦役が担う
意味分類をpath規則へ置き換える。案Cは入力が一件増えるが、既存のGit差分を正本として全pathの
一致を機械照合できる。

v1 §4.1以降の入力、出力、警告と停止の境界は、案Cの説明としてそのまま維持する。

## 3. 修正後確認の限定

一回の修正後確認は、次の二点だけを対象とする。

1. 案Aが新規実装を伴わない既存機能・設定だけの最小案になったか。
2. 案Aとの比較後も、案Cの選択理由が上位方針と承認済み判断に整合するか。

新しい問題を示す直接証拠がない限り、v1の変更していない節を再レビューせず、実装細部や追加機構を
開始条件にしない。修正後確認で止める指摘を解消できなければ、実装へ進まず利用者へ返す。
