# 第3段 G06共通内容識別値・経路・出力の再評価 軽量作業票 v1

- 作成日：2026-08-13
- 基準commit：`9dfe202`
- 対象Issue：`ISSUE-TEST-GROWTH-STATE-PINNING-001`
- 危険度案：高。多数の現在処理が共有する境界を扱う。ただし読み取りと候補作成だけなので独立完了レビュー一回に限定する
- 作業担当：操縦役
- 完了レビュー担当：新規サブエージェント一者
- 外部モデル確認：行わない。第3段完了前の一回を残す

## 1. 目的

G06追加24試験について、内容識別値のJSON互換性と経路の同一実体判定という二つの現在保証を確認し、
各入力例の固有性、重複、現在利用者、実在文書・台帳との互換性を意味的に完結する一群で再評価する。
固定値や暫定注記だけで履歴専用とせず、削減件数を先に目標にしない。

## 2. 入力と根拠

- `records/development/2026-08-13-test-growth-nodeid-candidates-v1.txt`
- `records/development/2026-08-13-stage3-test-cleanup-semantic-grouping-evidence-v1.md`
- `records/development/2026-08-13-stage3-mixed-groups-history-pin-extraction-evidence-v1.md`
- `tools/common/digests.py`
- `tools/common/paths.py`
- `tools/common/errors.py`
- `tools/common/output.py`
- `tests/test_common_digests.py`
- `tests/test_common_errors_paths_output.py`
- 現在の共通処理を取り込む`tools/`配下の処理

履歴上の主要commitは、共通化`a87ac66`と`38a5eca`、重複整理`8b19508`、G06試験追加`a84b8ca`、
実装修正`b20d76b`、NaN試験の修正`9461f34`とする。

## 3. 作業範囲と対象外

次を読み取りとリポジトリ外の一時複製による機械照合だけで行う。

- G06追加24試験、関連する既存試験、共通処理、その現在利用者を列挙する。
- 内容識別値の各不正入力を、非文字列key、配列でない連続値、非有限数、集合、bytes、入れ子へ意味分類する。
- 経路の各入力を、同一root、子、表記差、存在しない内外、実際の外部へ意味分類する。
- 現在の実在文書・台帳の内容識別値互換性を確認する。
- 安全条件を意味分類ごとに一つずつ壊し、どの試験が検出するか確認する。
- 現状維持、同じ試験関数内の入力例縮小、共有処理・利用者・試験の同時整理、の三案を比較する。

コード、試験、設定、文書、台帳、TODOは変更しない。新しい試験・検査器・台帳・入口、外部送信、Claude確認、
全試験、G06外、第3段完了判断は対象外とする。

## 4. 期待する成果

一件の再評価Evidenceに、24件の意味分類、現在利用者、関連試験、実在資料との互換性、一時変異の結果、重複、
四分類、三案比較、利用者が判断する点、未実施をまとめる。試験の組合せを減らせる場合も、安全境界を維持した
同じ試験関数内の入力例縮小として示し、直ちに変更しない。

## 5. 機械確認

- `rg`とPython構文木で取り込み、別名束縛、呼出し、試験識別子を列挙する。
- `git log`、`git show`、`git diff`で主要commitと現在の差を確認する。
- G06追加24件と関連試験を単独の試験コマンドで実行し、終了コードを確認する。
- 現在の実在JSON記録を読み、宣言内容識別値と共通処理の再計算を照合する。
- 一時複製では一つの意味条件だけを変更し、対象試験の終了コードで検出を確認する。
- 成果物はSHA-256、再読込み、参照照合、`git diff --check`で確認する。

## 6. レビューで判断する事項

- 24件だけでなく、共通処理、現在利用者、既存試験、実在資料を同じ意味単位で扱っているか。
- 先行調査で確認済みの「固定値試験は現在互換性」を繰り返さず、今回新たに重複と検査能力を確認しているか。
- 同じ関数の引数違いを独立した保証数と数えず、意味条件ごとに評価しているか。
- 変異が一つの意味条件だけを壊し、別原因を混ぜていないか。
- 実行時間や削減件数を中心理由にせず、理解、修正、復旧、誤判定の総費用を比較しているか。
- 本質から外れた新機構や過剰な修正案を提示していないか。

## 7. 停止条件と完了条件

停止条件：

- 現在の共有境界、正式入口、台帳、正本を直ちに変更する必要が判明する。
- コード、試験、設定、文書、台帳の変更が必要になる。
- G06外の欠陥修正、別群、新機構へ連鎖する。

完了条件：

- G06追加24件、関連試験、共通処理、現在利用者、実在資料が機械照合される。
- 入力例を意味条件へまとめ、固有保証と重複が示される。
- 代表的な正常例、異常例、境界例と一条件ずつの一時変異が記録される。
- 四分類と三案比較が根拠付きで示される。
- 一回の独立完了レビューが結果を確認する。
- 実施や意味変更は利用者判断へ分離される。
