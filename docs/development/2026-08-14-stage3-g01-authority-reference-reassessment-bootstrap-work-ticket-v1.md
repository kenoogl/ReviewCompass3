# 第3段 G01権威参照と内容識別値の再評価 軽量作業票 v1

- 作成日：2026-08-14
- 基準commit：`82522f2`
- 対象Issue：`ISSUE-TEST-GROWTH-STATE-PINNING-001`
- 危険度案：高。現在有効な上位文書の参照改変を検出する守り役を扱う。ただし読み取りと候補作成だけなので、独立完了レビュー一回に限定する
- 作業担当：操縦役
- 完了レビュー担当：新規サブエージェント一者
- 外部モデル確認：行わない。第3段完了前の一回を残す

## 1. 目的

G01の19試験について、上位文書の参照先とSHA-256（内容から計算する識別値）を照合する現在保証を確認し、
各正常例、異常例、境界例の独自性、重複、現在の利用先、正式な使用経路を再評価する。検査コードの
`provisional`（暫定）・`non-normative`（正本ではない）という宣言も含め、通常の製品コードからの呼出しが
ないことだけで役割終了とせず、削減件数を先に目標にしない。

## 2. 入力と根拠

- `records/development/2026-08-13-test-growth-nodeid-candidates-v1.txt`
- `records/development/2026-08-13-stage3-test-cleanup-semantic-grouping-evidence-v1.md`
- `records/development/2026-08-10-authority-reference-checker-green-evidence-v1.md`
- `records/session-handoffs/2026-08-10-codex-review-result-authority-reference-checker-v2.md`
- `records/session-handoffs/2026-08-10-claude-pilot-reference-digest-checker-scope-v2.md`
- `tools/development/authority_reference_checker.py`
- `tools/development/authority_reference_keys.json`
- `tests/test_authority_reference_checker.py`
- 現在の文書、案内、設定、コードにある検査コードと許可一覧への参照

履歴上の主要commitは、初期RED `c3bcb0f`、初期実装 `f61eeca`、追加RED `2914e39`、追加修正
`f07d94b`とする。実際のcommit列はGitから再確認し、記録の記述と照合する。

## 3. 作業範囲と対象外

次を読み取りと、リポジトリ外の一時複製による機械照合だけで行う。

- 19試験を正常例、異常例、境界例と意味条件へ分類する。
- 検査コードと許可一覧を呼ぶコード、設定、案内、文書、実行記録を列挙する。
- Human承認済み7キー、単一参照と複数参照の二形、経路安全性、実在、内容一致、空合格禁止の保証を対応付ける。
- 同一の意味条件を複数入力が確認している箇所と、入力ごとの独自保証を分ける。
- 代表的な条件を一つずつ壊し、どの既存試験が検出するかを確認する。
- 現状維持、意味条件を保った試験入力の縮小、検査コードの位置づけを含む同時整理、の三案を比較する。

コード、試験、設定、許可一覧、既存文書、TODOは変更しない。新しい試験・検査器・台帳・入口、実文書の
修復、外部送信、Claude確認、正規全試験、G01外、第3段完了判断は対象外とする。

## 4. 期待する成果

一件の再評価Evidenceに、19件の意味分類、現在の利用先、正式な使用経路、保証対応、代表的な一時変異、
重複、四分類、三案比較、利用者が判断する点、未実施をまとめる。縮小可能性があっても直ちに変更せず、
検査コード・許可一覧・試験・実文書の関係を意味的に完結する単位で示す。

## 5. 機械確認

- `rg`とPython構文木で取込、呼出し、操作入口、案内、試験識別子を列挙する。
- `git log`、`git show`、`git diff`で主要commitと現在の差を確認する。
- G01の19件を単独の試験コマンドで実行し、終了コードを確認する。
- 過去に正式使用された実文書と現在の有効文書を、検査コードの単独コマンドで読み取り確認する。
- 一時複製では一つの意味条件だけを変更し、既存試験の終了コードで検出を確認する。
- 成果物はSHA-256、再読込み、参照照合、`git diff --check`で確認する。

## 6. レビューで判断する事項

- 19件だけでなく、検査コード、許可一覧、現在の利用先、実文書を同じ意味単位で扱っているか。
- 暫定・非正本という宣言と、実際の使用実績・現行導線を混同していないか。
- 同じ試験関数の引数違いを独立した保証数と数えず、意味条件ごとに評価しているか。
- 一時変異が一つの意味条件だけを壊し、別原因を混ぜていないか。
- 実行時間や削減件数を中心理由にせず、理解、変更、調査、復旧、誤判定の総費用を比較しているか。
- 本質から外れた新機構や過剰な修正案を提示していないか。

## 7. 停止条件と完了条件

停止条件：

- 現在有効な上位文書の参照改変を検出できない重大な欠陥が見つかる。
- コード、試験、設定、許可一覧、実文書の変更が必要になる。
- G01外の欠陥修正、別群、新機構へ連鎖する。

完了条件：

- G01の19件、検査コード、許可一覧、現在の利用先、実文書が機械照合される。
- 正常例、異常例、境界例を意味条件へまとめ、固有保証と重複が示される。
- 代表的な条件を一つずつ壊した結果が記録される。
- 四分類と三案比較が根拠付きで示される。
- 一回の独立完了レビューが結果を確認する。
- 実施や意味変更は利用者判断へ分離される。
