# 一件レビュー材料作成・結果整理 製品TDD実装作業票 v2

- 作業票ID：`RC3-ONE-ITEM-REVIEW-IMPLEMENTATION-2026-08-15-V2`
- 作成日：2026-08-15
- 基準commit：`1109f2df4dc16e2466d3a46b2037dd7967e4587e`
- 採用契約：`records/task-contract/2026-08-15-one-item-review-material-and-result-organization-candidate-v3.md`
- 契約SHA-256：`a52cd717f6709c5ca01a1e339385272abfe976a0b9ce176e857b427778cf07d6`
- 利用者判断：`records/development/2026-08-15-one-item-review-contract-adoption-and-implementation-start-decision-v1.md`
- 訂正根拠：`records/development/2026-08-15-one-item-review-implementation-start-review-v1.md`
- 状態：`tdd_boundary_precheck_candidate_v2 / independent_correction_review_pending`
- 危険度：`high`

## 1. 目的と実装上限

【判断】採用済み契約v3に従い、資料一件の固定材料作成と、手元にある結果集合の整理を、外部送信・外部処理・
保存なしで行う。利用者が全結果を横断せず、証拠不足、競合、重複可能性、単独報告、一致報告の順に判断できる状態を作る。

変更を許すのは次だけである。

- 新規：`tools/reviews/one_item_review.py`
- 新規：`tools/reviews/one_item_review_entry.py`
- 変更：`pyproject.toml`へ`reviewcompass3-one-item-review`一件
- 新規：`tests/test_one_item_review.py`
- 本作業の証拠・レビュー記録とTODO

既存G02の14 file、既存試験、安全保存、G20、G30は変更しない。

## 2. 実装方法

【判断】契約v3 §13の案Cを使う。純粋な計算と安全なfile読取りを製品核へ置き、引数処理とJSON表示だけを入口へ置く。
G25の公開`default_pattern_rules`と`find_high_entropy`だけを再利用し、環境値解決、伏字化、報告file書込みは呼ばない。
既存G02は外部実行・raw保存・二担当経路へ結合しているため変更しない。

## 3. TDD境界

### 境界1：一件だけの安全な入力読取り

- 利用者から見た意味：明示したroot内の資料と条件だけを読み、他のfileを探索しない。
- RED：製品核が存在しないため、正常な一件の読取りが失敗する。実装後は、相対path、root外、途中symlink、通常file以外、
  同一file指定、空本文、NUL、UTF-8不正、各size上限超過を固定理由で拒否する。
- 最小実装：絶対pathを検査し、rootからfileまでsymlinkを追わずfile descriptorで開き、open後の種類とsizeを再確認して読む。
- 先取りしない責務：schema、機微情報、材料JSON、結果整理、CLI表示はまだ実装しない。
- 不変条件：file書込み、directory探索、環境値解決、入力pathの返却は0回。
- 前提：なし。後続：境界2。戻せる地点：安全なbytes三種を値で読める核。

### 境界2：固定レビュー材料の作成と安全停止

- 利用者から見た意味：資料一件、目的、基準、制約を、同じ入力なら同じ内容識別値を持つ材料へする。
- RED：正常な資料と条件から契約§8.2の完全一致schemaを得られず失敗する。秘密pattern、高乱雑性token、
  POSIX・drive・共有・`file://`を本文、表示名、ID、keyへ入れると、値を出さず停止することも先に固定する。
- 最小実装：条件schemaを厳格検査し、G25の二公開関数と契約固定4 patternで資料本文と復号後の条件JSONの全key・値を検査し、
  順序と正準JSONを固定する。自己SHA欄を除く正常結果から`material_package_sha256`を計算する。
- 先取りしない責務：結果集合の解析、結果集合の安全検査、所見整理。
- 不変条件：rootと入力fileの絶対path、検出値、例外本文を結果へ入れない。位置指定引数は内容検査とSHA対象外。
- 前提：境界1。後続：境界3。戻せる地点：材料作成関数が正常・停止を値で返す。

### 境界3：結果集合の厳格検査、安全停止、材料への束縛

- 利用者から見た意味：一件から八件の結果が、現在の材料について契約schemaどおりで、秘密候補や絶対pathを含まない場合だけ整理へ進める。
- RED：別材料SHA、未知key、未知基準、重複ID・基準ID、範囲外行、空結果、`verdict`と件数の矛盾、100指摘超過を拒否できず失敗する。
  結果集合のsummary、title、description、全識別子、全keyを含む復号後の全文字列key・値へ、秘密pattern、高乱雑性token、
  POSIX・drive・共有・`file://`の各候補を一つずつ入れ、値、一致箇所、入力pathを出さず停止できない例も失敗として固定する。
  集合扱いの配列を逆順にした同じ意味の入力でSHAが変わる例も失敗として固定する。
- 最小実装：全root・入れ子keyを完全一致で検査する。SHA-256欄は形式と参照一致を先に確認し、その欄だけを高乱雑性検査から除外する。
  それ以外の復号後の全key・文字列値へ、境界2と同じG25二関数と契約固定4 patternを適用する。その後、review、finding、基準IDを
  契約順へ正規化して、`review_sha256`、`review_content_sha256`、`result_set_sha256`を計算する。
- 先取りしない責務：問題keyによる群分けと人の判断一覧。
- 不変条件：不合格時を含め入力result本文、検出値、一致箇所、入力path、例外本文を出力へ写さない。材料本文を整理結果へ渡さない。
- 前提：境界2。後続：境界4。戻せる地点：安全検査とschema検査を通った結果集合を値で得る。

### 境界4：一致・競合・重複と人の判断一覧

- 利用者から見た意味：担当別の結論と件数を失わず、確認対象を群単位の順序付き一覧で得る。
- RED：同じ問題keyの一致・競合・単独報告、担当IDだけ違う複製review、異なるkeyの完全一致、証拠不足を契約どおり整理できず失敗する。
  複製reviewを一致根拠として数える例と、`matching_reports`を人の判断一覧から外す例を必ず失敗させる。
- 最小実装：契約§8.4の指摘署名、群分け、重複検出、全配列順、判断一覧の固定区分順をそのまま計算する。
- 先取りしない責務：意味が似る別表現の統合、指摘採否、修正方針、完了判断。
- 不変条件：全群の元担当・指摘ID・severity・本文・基準・行範囲を保持するが、資料本文は返さない。
- 前提：境界3。後続：境界5。戻せる地点：整理結果を値で再生成できる。

### 境界5：製品入口、安全な表示、禁止作用0回

- 利用者から見た意味：導入後の一つの実行名から`prepare`と`organize`を使い、成功または安全な停止JSON一件を得る。
- RED：入口と実行名がないため正常呼出しが失敗する。未知引数、相対path、各核停止値、内部失敗で入力値・path・例外を出す例、
  標準エラーへ出す例、終了コード・末尾LF・key集合が違う例も失敗させる。
- 最小実装：薄い入口で引数を解決し、境界1〜4の製品核が返す成功値または停止値を、契約の正準JSON一件と固定終了コードへ変換する。
  結果集合のschema検査、安全検査、内容識別値計算はこの境界へ追加しない。
- 先取りしない責務：file保存、外部送信、外部処理、設定探索、複数資料。
- 不変条件：成功・失敗でfile書込み、network、外部process、Git、環境値解決、権限変更、削除は0回。
- 前提：境界4。後続：境界6。戻せる地点：正式実行名から二操作が動く。

### 境界6：結合・配置・回帰確認

- 利用者から見た意味：現在位置に依存せず、正常材料から整理結果まで一続きで再現できる。
- RED：導入後を模した別の現在位置から実行名を解決できず失敗する。入力を一項目変えた旧結果再利用、既存G02変更、
  repository・入力fileのbytes変化を検出する確認も固定する。
- 最小実装：新入口と`pyproject.toml`の実行名だけを接続する。新しい状態管理や保存は追加しない。
- 先取りしない責務：実レビュー実行、外部送信、結果保存、Human採否記録。
- 不変条件：既存G02 14 fileと既存試験の既定動作を変えない。
- 前提：境界1〜5。後続：対象・関連・全試験、独立完了レビュー、利用者受入。
- 戻せる地点：新しい2 code file、実行名、対象試験を外せば元に戻る。

## 4. 契約受入条件と境界の対応

| 条件 | 主境界 | REDまたは後続確認 |
| --- | --- | --- |
| 1 | 1、2 | 一件限定の読取りと§8.2完全一致schema |
| 2 | 2、6 | 時刻・環境値・現在位置・key順に依存しない同一bytes |
| 3 | 2、3、6 | 資料1 byte、条件・結果1項目の改変と旧SHA再利用拒否 |
| 4 | 1 | root外、symlink、非通常file、複数資料、size超過拒否 |
| 5 | 2、3 | 資料・条件・結果集合の全key・文字列値の秘密・高乱雑性・四path検査 |
| 6 | 3、4 | 結果1〜8件と担当別情報・元結果SHA保持 |
| 7 | 3 | 空集合、別材料、未知key・基準、範囲外行、結論矛盾拒否 |
| 8 | 4 | 同一keyの一致・競合・単独報告分類 |
| 9 | 4 | 異なるkeyの完全一致だけを重複候補化 |
| 10 | 3、4 | 担当IDだけ異なる同一reviewの検出と一致根拠除外 |
| 11 | 4 | 全区分を固定順でHuman queueへ保持 |
| 12 | 3、4 | 結果集合の事前安全停止と本文・path・入力外判断の出力禁止 |
| 13 | 3、4 | 集合扱い配列の逆順でも同一SHA・同一整理結果 |
| 14 | 5 | 成功・停止JSON bytes、key、配列順、末尾LF、stderr |
| 15 | 1、5 | 全経路の書込み・network・外部process・Git・環境値解決・権限変更・送信0回 |
| 16 | 5、6 | 正式入口を導入後の別現在位置から実行 |
| 17 | 6 | 対象、G02関連、安全出力関連、正規全試験を単独commandで実行 |
| 18 | 6の後続 | 独立完了レビューと合成一件の利用者受入 |

【判断】条件別の主接続は全18件、未接続0件である。条件5と12は`prepare`だけでなく、結果集合を読む`organize`の境界3へ接続する。
条件18の利用者受入は自動試験で代替せず、境界6完了後のHuman判断として残す。

## 5. 実装順序とコミット

1. 本訂正版を独立変更点レビューし、開始可になる。
2. 境界1から順に、対象試験へその境界のREDを追加し、実装なしで主要理由一つにより失敗することを確認する。
3. 試験を変えず、許可path内の最小実装だけでGREENにする。
4. 各境界のRED・GREEN command、終了コード、件数、変更path、commitを短い証拠へ残す。
5. 対象試験、影響する既存G02試験、安全出力関連試験、正規全試験を単独commandで実行する。
6. 高危険度反例を独立判定し、契約適合確認と最終挑戦を一回の独立完了レビューで行う。

【判断】意味単位は、契約schemaが途中で矛盾しない範囲で境界ごとに完結させる。後続境界で先行試験の期待結果を
書き換える必要が判明した場合は停止する。Pythonは4スペースとする。

## 6. 開始前レビュー基準

1. 六境界が利用者から見た意味を持ち、一つの主要RED理由と最小実装へ対応する。
2. §4の対応表で契約受入条件1〜18に未接続がなく、同じ条件を複数境界へ不必要に重複させない。
3. 機微情報、絶対path、改変、複製、配列順、出力bytes、禁止作用を実装前に反証できる。
4. 既存G02を変更せず、G25の二公開関数だけを限定再利用する。
5. 変更上限、停止条件、戻せる地点、製品入口への公開時点が一意である。

開始可、止める指摘0件となるまで、製品試験と製品コードを変更しない。

## 7. 完了条件

- 六境界の対象試験が実装なしで失敗し、試験を変えず最小実装で成功した証拠がある。
- 契約受入条件1〜18、対象・関連・全試験、禁止作用、配置確認が成功する。
- 独立完了レビューが`verified`となる。
- 利用者が合成一件の材料と整理結果を確認し、製品処理として受け入れる。

## 8. 未実施

【未実施】本訂正版作成時点では、製品code、製品試験、`pyproject.toml`、既存G02を変更していない。
実利用者資料、外部送信、外部処理、保存、削除、正規全試験、push、tag、履歴書換えも行っていない。
