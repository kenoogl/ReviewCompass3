# RQ2実験計画の起草 作業票 v1（範囲固定・データ取得順序4前段）

- 作成日：2026-08-17
- 指示者：利用者（Human）。選択文言「順序4の実験計画の起草に着手。範囲固定文書から進めて」
  （2026-08-17 chat）
- 作成者：Claude
- 種別：範囲固定文書（軽量作業票）。**承認までは着手しない**
- 上位計画：評価データ取得計画v1 §3優先1b・§4順序4。固定入力＝事前走査record
  `records/development/2026-08-17-rq2-experiment-plan-prescan-v1.md`

## 1. 目的

paired trial（RQ2：Context scalability）の**実験計画recordを起草**する。この計画recordは
バッチ起動承認（順序3作業票で確定した形——計画の事前承認をもってバッチ内の個別起動を委任）の
**承認対象文書**であり、承認すればそのまま実験を実行できる粒度（ケース一覧・条件・起動回数上限・
費用見積り・正解固定の手順・実行手順・中断条件）まで固める。

## 2. 正本範囲（成果物）

1. **実験計画record草案**：`records/development/`へ新設。内容：
   - ケース一覧（供給方針・各ケースの対象と正解Findingの固定方法）
   - 条件設計（A／B／C／Dの定義と各ケースへの適用）
   - 起動回数の上限・費用見積り（実測6.6万トークン/起動を基礎に）
   - 実行手順（bridge経由・record命名`rq2-case-NNN-`系・計測は自動）と中断条件
   - 正解Finding固定の人手作業一覧（利用者の関与量を明示）
2. 本作業は**文書起草のみ**。装置実装・ケース材料のcommit・実起動は計画承認後の後続作業単位。

## 3. 起草時の設計方針（Human確認点・推奨つき）

1. **ケース供給＝実欠陥再構成＋人工欠陥の混合（推奨）**：
   - 実欠陥：本日の`dequeue`見落とし（正準列定義の実物不一致）等、実開発で起きた欠陥の再構成
     ——「埋込欠陥の代表性」批判（RC2調査）への対策
   - 人工欠陥：Basili系の設計（欠陥分類・正解集合の改訂手順）で対象文書へ注入
   - 合格系ケース（既存verdict素材）は誤検出・責務外Finding率の測定用に少数含める
2. **ケース数＝8〜12・条件＝B／C核心優先（推奨）**：B（Task Contract方式）vs C（＋無関係資料）
   の全ケース対比を主実験（入力安定性の核心図）とし、A（ベースライン）・D（欠落negative）は
   各2〜3ケースの副実験に縮小。起動数の目安：8ケース×2条件＋副実験6起動＝**約22起動・
   約145万トークン**（フル4条件60起動・390万トークンとの比較で提示）
3. **正解Finding固定＝利用者レビューつき（推奨）**：Claudeが正解案（欠陥位置・期待Finding）を
   起草し、利用者が確定する2段階（人手作業の見積りを計画に明記）
4. 費用・回数上限：計画recordに絶対上限を明記し、超過時は停止して再承認

## 4. 受入条件

1. 実験計画record草案が§2-1の全項目を含み、**バッチ承認可能な粒度**（承認文言だけで実験開始
   できる）に達している。
2. 見積り値（起動数・トークン）が実測基礎（6.6万/起動）からの機械計算である。
3. `git diff --check`・意味単位commit・`work_unit_transition`合格。
4. 計画の**承認は本作業の範囲外**（起草完了の報告後、利用者が判断）。

## 5. 着手後の手続き（承認後の順序）

1. 実験計画record草案の起草→commit。
2. 起草内容の提示→計画承認の判断へ（承認されれば後続：装置実装→ケース材料commit→正解固定→
   実起動バッチ→RQ2集計）。

## 6. 範囲外

- paired evaluation装置の実装・ケース材料の作成・正解Findingの確定・実起動。
- 実験計画の承認そのもの（利用者判断）。
