# 統合評価（評価②）の定義と進め方 提案 v2

- 状態：`human_decision_pending`（Human承認まで統合作業を開始しない）
- 作成日：2026-08-08
- 承認根拠：`DEC-EGRESS-METHOD-CONCLUSION-001`（Human「統合へ進む」）
- v1（SHA-256 `298fafb649c00dfa5eda4b6a3fa9c7dd998cc68059c510f2beb8d4cb26e0d5ba`）を差し替える。
  変更はHuman指摘2件の反映のみ：テスト修正の3分類（判断点1への指摘）と、テスト重複の
  issue化経路（「テスト自体の重複整理が必要なら、後に整理。issueに挙げるのがよいか？」→承認）。
  v1は歴史として保持する
- 判定の入力：B確定判定（same 48組、`DEC-EGRESS-B-CHECK-001`）と100%一致record

## 0. 提案前の確認（実測）

- 統合除外宣言（`RC3-INTEGRATION-EXCLUSIONS-001`）は順位表v2の生成時に適用済み。
  対象系統は除外を通過した候補のみで構成される（系統ごとの再照合は手順①で行う）
- **守り役moduleとの重なり（prefix照合で実測）**：SHA-256系4/7file、fail-closed例外系3/7、
  canonical digest系4/4、`_within`系4/4（`tools/session_logs/`全体が守り役指定）、JSON印字系2/3。
  **統合はほぼ全系統で守り役codeの変更を伴う**
- 関連する登録済み課題：`ISSUE-UNREVIEWED-WORK-REVIEW-BACKLOG-001`（守り役への後追い独立
  レビュー未実施）。統合対象の守り役はテスト先行の統合作業と反証レビューを通るため、
  当該fileに限り実質的な後追いレビューを兼ねる（課題自体の解消はHuman判断のまま）

## 1. 評価②の定義

**問い**：評価①でsame（そのまま置き換え可能）とされた部品群を、実際に1つへ統合すべきか。

**判断材料**（すべてローカル実測。`DEC-EGRESS-B-CHECK-001`の基準を継承）：

1. 使われ方：呼び出し箇所数・module数・テスト参照（機械集計）
2. 効果：削減行数・重複箇所数の実測、見通しの向上（Human文言「コード量が大幅に減る、
   見通しが良くなる」2026-08-08）
3. 保証への影響：型・名前の区別が守っている不変条件が消えないか（#9・#2の教訓）
4. 名前・契約：外部互換（コードベース外）に触れないか（#10・案イの教訓）
5. risk：守り役codeか、凍結レーンに触れないか、テストの有無

**出力**：系統ごとに「統合する／しない／保留」＋統合の形。**決定者はHuman**
（材料と所見コメントはClaudeが用意する。DEC-EGRESS-B-CHECK-001 §1の手順どおり）。

## 2. 対象6系統と初期見積り

| 系統 | 規模 | 統合の形（案） | 初見 |
| --- | --- | --- | --- |
| A. SHA-256計算 | 7file逐語重複 | 共通module（例：`tools/common/digests.py`）の関数1つへ寄せる | 効果明確 |
| B. fail-closed例外 | 7class逐語重複 | **共通基底クラス**を新設し各classは継承1行に。**各moduleのクラス名は残す**（捕捉挙動不変。案イと整合） | 効果明確 |
| C. canonical digest計算 | 4file逐語重複 | Aと同じ共通moduleへ | 4/4が守り役。慎重に |
| D. `_within`パス判定 | 4file逐語重複 | session_logs内の共通moduleへ | 全て守り役 |
| E. JSON印字 | 3file逐語重複 | 共通moduleへ | 効果小さめ |
| F. dataclass系 3組 | 逐語でなく構造一致 | 個別判断（Policy/Universeは統合対象外と裁定済み） | 保留寄り |

## 3. 手順（1系統1単位。順に完結させる）

1. **照合**：対象fileを統合除外宣言・凍結レーン・守り役一覧とprefix照合し、結果を記録
2. **使われ方の実測**：呼び出し箇所・テスト参照を機械集計。**テストが対象symbolを直接参照して
   いるか、テスト自体の重複があるかも集計**し、統合案に「無修正で通る／aliasで無修正を保てる／
   参照替えが要る」を明記してHuman承認の材料に含める
3. **統合案と効果見積り**：形（共通module／基底クラス）、削減行数の機械集計、
   保証・名前への影響の所見コメント
4. **Human承認**：系統ごとに「統合する／しない／保留」と形を承認
5. **TDD実施**：共通部品のテストを先に書き、RED確認→実装→**既存テスト無変更でGREEN（既定）**。
   既存テストの扱いは3分類とする：
   - **挙動の変化を吸収するための修正は禁止**。テストを直さないと通らない場合は統合を保留し、
     挙動差をHumanへ報告する（既存テストは挙動不変の証明器であり、警報として扱う）
   - **実装詳細への結合による機械的な参照替えは条件付きで許容**。第一選択はalias（別名1行）を
     残して無修正を保つ。やむを得ない場合のみ、assertを一切変えず参照だけを更新し、
     独立したcommitに分離する
   - **テスト自体の重複整理は範囲外**（blocker「テストの一斉整理は行わない」を尊重）
6. **回帰確認**：全suite合格、`git diff --check`
7. **反証レビュー**：守り役に触れた系統は`work-review-protocol` §3の`high`で反証を新作
8. **Evidence固定・コミット**：系統ごとにGREEN Evidenceを固定

## 4. 統合の原則

- **挙動不変**：公開の名前・例外の捕捉名・出力形式を変えない。変える必要が出たら
  その系統を保留にしてHumanへ
- 共通moduleの置き場と命名はA系統の承認時にHumanが決める（LLMは仮案を出すのみ）
- 統合と機能変更を同じcommitに混ぜない

## 5. 非対象

- 評価①の再判定、順位21位以下への探索拡大
- Policy/Universe（統合対象外と裁定済み）と#9〜#12等のdifferent 11組
- 統合の自動実行（各系統にHuman承認が要る）
- 外部送信（A2は中止済み）

## 6. risk

- 守り役codeの変更を伴うため、系統ごとの反証レビュー（§3手順7）を必須とする。
  反証観点の例：統合後に例外の捕捉範囲が変わっていないか、digest計算の出力が
  1bitも変わっていないか（統合前後で全対象の出力を機械比較する）
- 未レビューの守り役に触れる件は§0の関連課題として記録済み
- 反証観点に「**テストの変更が挙動のassertに触れていないこと**」を加える（参照替えを行った系統）
- **テスト重複の扱い**：手順2の実測で重複が実在すれば、観測記録→改善候補→Humanトリアージの
  既存経路でissue化する（`registered / nonblocking`想定。着手はHuman判断）。本統合作業では
  整理しない

## 7. Human判断点

1. 評価②の定義（判断材料5点・出力・決定者）
2. 1系統1単位の手順8段（とくに守り役への反証レビュー必須）
3. 統合の原則（挙動不変・混在commit禁止）
4. **着手順の案**：A（SHA-256）→B（fail-closed例外）→C以降。Aが最小で効果測定に向く
