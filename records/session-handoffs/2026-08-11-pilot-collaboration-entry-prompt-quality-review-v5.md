# 操縦者別連携 単一v5指示文品質確認

- 日付：2026-08-11
- 対象コミット：`3891c310f93ecb7561977dbd7dd73c801ed00224`
- 対象指示書：`records/session-handoffs/2026-08-11-pilot-collaboration-entry-implementation-request-v5.md`
- 対象指示書SHA-256：`bd7ed0ce11c3a9cf75a350d17644007eb1da0c24bb26c2725c32229db715f81a`
- 監査モデル：`gpt-5.6-terra`
- 判定モデル：`gpt-5.6-terra`（監査とは別の新しい会話状態）
- 未加工結果保存：`specified_only`。最終応答は主担当の会話で受領したが、不変保存処理は未接続
- 現在状態：`Human判断待ち`

## 1. 機械確認

コミット内blobのSHA-256は指定値と一致した。v5本文から要求識別子を機械抽出し、受入条件9件、禁止事項
7件、停止条件4件、出力要件6件の計26件と一致することを確認した。欠落と余分は0件だった。

## 2. 監査結果

監査担当は新規所見`PA-PC-010`を1件出し、総合`revise`とした。`PA-PC-008`は`closed`、`PA-PC-009`は
`open`と判定した。

| ID | 重大度 | 影響 | 事象 | 推奨修正 |
| --- | --- | --- | --- | --- |
| `PA-PC-010` | high | `AC-PC-003`, `AC-PC-004`, `AC-PC-005`, `AC-PC-006`, `AC-PC-007`, `AC-PC-008`, `ST-PC-004`, `OUT-PC-003` | `envelope_sha256`はそのkeyを除く正規JSON bytesのSHA-256だが、`input_envelope_sha256`は実際に呼出しへ渡したenvelope file bytesのSHA-256と読める。保存fileには`envelope_sha256`自身と末尾改行が加わるため、両値は同一にならない。正しい入力でも常に入力不一致となり得る | 実際に呼出しへ渡すpayloadを`envelope_sha256` keyを除く正規JSON bytesへ固定し、起動記録、raw、event、再導出で同じ内容指紋を比較する。保存file全体を渡すなら内容指紋とfile全体指紋を別keyに分ける |

## 3. 独立判定結果

判定担当は監査結果と`PA-PC-010`の使用を`accept`推奨とした。最小構成の反証計算でも、keyを除く内容指紋と
保存file全体の指紋が一致しないことを確認した。新規所見は追加しなかった。

- `PA-PC-008`：`closed`
- `PA-PC-009`：`open`。入力束縛の値は追加されたが、比較対象が一意でないため解消未了
- 実装担当の起動：不可

## 4. Human判断境界

`PA-PC-010`の採用、棄却または保留をHumanが裁定するまで、v5を変更せず、実装担当を起動しない。採用する
場合は、実際に渡すpayloadとそのSHA-256計算対象を一意にした新しい単一指示書を作り、新しいSHA-256へ固定して、
別の新しい会話状態で監査と判定を再実行する。
