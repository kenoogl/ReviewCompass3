# 第5段 G25 Session成果物入口 正式・安定表示への昇格Evidence v1

- 実施日：2026-08-14
- 対象：`tools/session_logs/read_only_entry.py`の成熟度表示三項目
- 昇格判断：`records/development/2026-08-14-stage5-g25-session-artifact-maturity-promotion-decision-v1.md`
- 判断SHA-256：`b0529f44d202c4b9c49600624417a54a611ad8eb77581ee9515c941291f850d1`
- 昇格commit：`f2bf9a7`
- 状態：`implementation_complete_pending_independent_review`

## 1. 開発物の機能と用途

対象の製品入口は、利用者が許可した一件のローカルSession記録を読み、伏字化した会話記録、要約、元記録との
対応情報を画面へ一回返す。過去作業の確認、引継ぎ、レビュー、調査に使う。

今回変更したのは、機能ではなく成熟度の表示である。「利用者に受け入れられ、現在の契約と試験を満たした正式な
製品入口である」ことを、製品入口の先頭で宣言する。

## 2. 実施した変更

【実測】`tools/session_logs/read_only_entry.py`の先頭三行だけを次のように変更した。

- `lifecycle: provisional`から`lifecycle: stable`へ変更した。
- `normative_status: non-normative`から`normative_status: normative`へ変更した。
- `promotion_required: true`から`promotion_required: false`へ変更した。

【実測】変更commitは上記file一件、3行追加、3行削除だけである。引数、入力処理、三形式の解析、伏字化、要約、
来歴、安全な出力選択、絶対path検査、停止結果、終了コード、配布入口は変更していない。試験、設定、契約、
G25固定10 path、G26、G30、他142 pathも変更していない。

## 3. 実装案の選択

【記録】昇格判断§2で三案を比較し、既存の製品入口先頭三行だけを更新する案Bを採用した。判断記録だけで
実表示を変えない案Aは不一致を残す。別設定を追加する案Cは、設定、参照処理、試験という不要な保守対象を増やす。

【判断】成熟度三項目は実行時の出力ではなく、人と開発処理が読む宣言である。今回の値だけを固定する新試験は
追加しなかった。変更後の直接再読込みと、既存の対象・関連・全試験で、表示と機能不変を確認した。

## 4. 機械確認

| 確認 | 結果 | 終了コード |
| --- | --- | --- |
| 変更後の三項目を直接再読込み | `stable / normative / promotion_required: false` | 0 |
| 新入口の対象試験 | 12件成功 | 0 |
| G25直接関連14 file | 55件成功 | 0 |
| 正規全試験 | 1,740件成功、失敗0、error 0、skip 0 | 0 |
| `git diff --check` | 指摘0件 | 0 |

【実測】正規全試験はPython 3.13.14、pytest 8.4.2、runner版2、代替実行なしだった。リポジトリ外の
受領記録は`/private/tmp/reviewcompass-stage5-g25-promotion-full-receipt.json`、SHA-256は
`ac13868e527b57fa439d7c79bdb8c1e260271771a37820f4091c61e9bdad7b97`、状態識別値は
`16ea8aa7023f82decc21d7177a0d146fe914eb019541a396fa9f1f81ffa719f2`である。

【実測】昇格commit後、同じ規則で作業ツリーの状態識別値を再計算した。結果は
`16ea8aa7023f82decc21d7177a0d146fe914eb019541a396fa9f1f81ffa719f2`で受領記録と完全一致した。
受領記録は、Evidence作成前かつ昇格commit後の変更なし状態へ直接結び付く。

## 5. 維持した安全上の限界

- 入力は利用者が許可したローカルSession記録一件だけである。
- 保存、探索、複数file処理、外部送信、ネットワーク通信、外部処理、Git操作、権限変更を行わない。
- 出力を外部送信許可済みとして扱わない。
- すべての機微情報を必ず検出するとは保証しない。
- 正式・安定になっても、ReviewCompass3自身の全作業へ直ちに必須適用しない。

## 6. 手戻り

【実測】正規全試験の最初の起動で、受領記録の引数名を`--receipt-output`と誤記し、引数解析が試験開始前に
終了コード2で停止した。正しい既存引数`--receipt`で再実行し、全試験1,740件成功となった。

- 対象操作：正規全試験と受領記録の作成。
- 期待した実行役：既存の`policy_test_runner`。
- 実際の実行役：同じ`policy_test_runner`。失敗原因は操縦役が引数名を取り違えたことである。
- 手作業理由：操縦役が既存入口の引数名を確認せず、過去の呼び方を誤って使用した。
- 証拠：最初の標準エラーは`--receipt`不足を示し、試験は開始されなかった。正しい再実行は終了コード0である。
- 機械処理候補：既存の`--help`と入口文書を開始前に参照する。今回の一度の誤記のために新しい仕組みや試験を
  追加しない。
- route：本作業内で訂正済み。製品欠陥、試験欠陥、追加作業候補にはしない。

## 7. 次と未実施

【判断】次は新規の独立担当が、三行だけの変更、利用者判断との一致、機能と限界の不変、試験受領記録と状態の
結び付きを確認する。

【未実施】第5段完了、G26、G30、他142 path、保存、探索、外部送信、環境値解決、不可逆操作、権限変更、
使用停止Issue処理、ReviewCompass3自身への必須適用は行っていない。新しい試験、設定、検査器、台帳、関門も
追加していない。
