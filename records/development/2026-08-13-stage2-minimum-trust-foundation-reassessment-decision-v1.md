# 第2段 最小信頼基盤の再判定・利用者判断 v1

- 記録ID：`DEC-STAGE2-MINIMUM-TRUST-FOUNDATION-REASSESSMENT-2026-08-13-V1`
- 判断日：2026-08-13
- 状態：`adoption_candidates_accepted_stage_completion_pending`
- 判断対象コミット：`403523d2eac44f1500184ff6eb69273ab1f5f61a`
- 再判定候補：`records/development/2026-08-13-stage2-minimum-trust-foundation-reassessment-candidate-v1.md`
- 再判定候補SHA-256：`30e8b70d26dfc41955867ad8b7d9a09b3215cbbe85be3916763b0d0e72ea7d08`
- Claude用指示文：`records/session-handoffs/2026-08-13-claude-stage2-minimum-trust-foundation-reassessment-review-prompt-v1.md`
- Claude用指示文SHA-256：`761863d37c4d99c473ef57825d7e2e9790a515fd2eae8d9dcc8bd3a3b4589456`

## 1. 判断

【記録】利用者は、二つの独立レビューの比較後に提示された次の案に対し、
「判断の証跡を残し、次へ進む」と指示した。

【判断】次を採用する。

1. 履歴保存、開発コード管理、テストコード管理、レビューの四領域を採用候補とする。
2. `CR-OTE-001`が示した静的Git検査は、`OUT-PC-006`全体の保証として無効と直ちに明示し、第2段完了の
   保証根拠に使わない。
3. Claude／Codexによる外部実装経路は、現在の`使用停止`を維持する。同経路を再び有効にする前に、
   実装担当の停止、push、履歴書換え、外部送信なしをどの境界と証拠で確認するか、別作業で再裁定する。
4. 上記の再裁定を、単に第3段へ見送ったとは扱わない。第3段で扱う場合も、外部実装経路の再開より前に
   完了することを停止条件とする。
5. 製品側`_run_git`への実行時の守り追加は、外部の実装担当に対する`OUT-PC-006`全体を保証しないため、
   現在作業の修正案にしない。同案の試行を放棄し、RED試験を取り消したcommit
   `89649713d99ec1edc86444e3e39cca4152972286`を追認する。
6. Python 3.13移行は当該外部実装経路を使わないため、先に進める。移行は第2段完了前の独立作業とし、
   移行が完了するまで第2段を完了にしない。

## 2. 二つの独立レビュー

| 項目 | 新規サブエージェント | Claude |
| --- | --- | --- |
| 対象 | 同じ再判定候補と固定材料 | 同じ再判定候補と固定材料 |
| 受け渡し | Codex内で新規実行単位へ直接委譲 | 外部送信制限のため利用者が固定指示文を手動で受け渡し |
| 判定 | `verified` | `verified` |
| 四領域 | すべて`採用候補` | すべて`採用候補` |
| 止める指摘 | 0件 | 0件 |
| 報告不一致 | 0件 | 0件 |
| Python 3.13 | 第2段完了前の必須条件 | 採用後の独立作業として分離可能。順序は利用者判断 |

【記録】新規サブエージェントは、動的に組み立てた名前から`_run_git(..., "push")`を呼ぶ反例を既存の
二つの静的検査へ渡し、command終了コード0、違反0件を再現した。そのうえで、再判定候補が同検査を第2段の
保証根拠から除外しているため、公式試験入口を採用候補とする判断は反証されないとした。

【記録】利用者が受け渡したClaudeの回答は、復旧結果記録と現在状態の結び付きを再計算し、
`source_state_digest`が`55638cce7660fff24dcfe87120ebd74a11257a365a8670a441ae8ac4c38a3c39`で一致したと報告した。
また、`git diff f85d70f 8964971`を対象試験fileと製品fileに限定して終了コード0、差分0と報告した。

【記録】Claudeの回答は利用者による手動受け渡しであり、Codexから外部送信していない。Claudeという実行主体の
同一性と生の応答はリポジトリ内の機械記録から独立確認できない。この制約を隠さず、利用者が提供した判断入力として
扱う。両レビューの主要結論は一致し、相違はPython 3.13の順序だけだった。

## 3. 相違点の裁定

【記録】上位計画v5の第2段完了条件はPython 3.13を明記していない。一方、元の採用表候補は、現行3.9系列の
保守終了と設定の3.9固定を根拠に、3.13移行を第2段で選ぶ試験入口の前提へ分類している。

【判断】利用者が以前に示した版更新方針、3.9を長期の信頼基盤として新規採用しないという既存候補、今回の
利用者指示を合わせ、Python 3.13移行を第2段完了前に置く。これは四領域の採用候補判定を取り消す意味ではない。

【実測】`OUT-PC-006`は、外部の実装担当が意味的に完結した変更をcommitして停止し、push、履歴書換え、
外部送信を行わないことを要求する。一方、`test_pilot_git_processes_are_read_only`が調べるのは、
`pilot_collaboration.py`内部の`_run_git`呼出しとprocess呼出しだけである。したがって、同試験だけでは
`OUT-PC-006`全体を保証できない。

【実測】第1段の現在位置表は、Claude／Codex連携の準備・取込みと信頼済み外部送信配置を`使用停止`とする。
Python 3.13移行の現在入口となる開発環境設定、依存固定、環境作成処理、公式試験設定には、外部実装経路を
起動する呼出しがない。`pyproject.toml`は`reviewcompass3-pilot`を導入対象として列挙するが、環境作成処理は
packageを導入するだけで同commandを実行しない。

【判断】このため、当該問題から直ちに外部送信または履歴変更が起きる現役経路はない。Python 3.13移行前に
製品側の守りを追加する必要はない。ただし、虚偽の保証は今この判断で無効とし、外部実装経路を再開する前の
必須対処として残す。再開前の再裁定なしに、同経路を`現役`へ変更してはならない。

## 4. 追加精査の機械確認

各commandは単独で終了コードを確認した。

| 確認 | 終了コード | 結果 |
| --- | ---: | --- |
| 現行製品sourceへ、実行しない構築名の`_run_git(..., "push")`を加え、既存の二つの静的検査へ渡す | 0 | 両検査とも違反0件。既知の偽陰性を再現 |
| v6実装指示から`OUT-PC-006`を検索 | 0 | 実装担当のcommit後停止、push・履歴書換え・外部送信禁止を確認 |
| 第1段現在位置表からClaude／Codex連携と信頼済み外部送信配置を検索 | 0 | 両経路とも`使用停止` |
| 環境作成処理、開発環境設定、依存固定、公式試験設定から外部実装経路の名前を検索 | 1 | 該当0件 |
| `pyproject.toml`と`setup.py`から`reviewcompass3-pilot`を検索 | 0 | `pyproject.toml`のcommand登録1件。実行呼出しではない |

## 5. 次の作業

【判断】次の一作業はPython 3.13移行の軽量作業票作成と開始確認である。移行作業では、現在のPython、
設定、依存固定、仮想環境作成入口、公式試験入口への影響を先に機械列挙する。Git検査の修正、重大な欠陥12件、
外部実装経路の再開、第3段の試験整理、第2段完了判断を混ぜない。

## 6. 未実施

【未実施】本判断は第2段完了、第3段開始、Python 3.13移行の実装、Git検査の修正、重大な欠陥12件の修復、
外部実装経路の再開、外部送信、履歴書換えを実施または承認しない。Claudeの回答本文をClaudeから直接取得した
とは主張しない。
