# 第5段 G25 Session成果物入口 正式・安定表示への昇格判断 v1

- Decision ID：`DEC-STAGE5-G25-SESSION-ARTIFACT-MATURITY-PROMOTION-2026-08-14-V1`
- 判断日：2026-08-14
- 判断主体：利用者
- 利用者の文言：`この入口を「正式・安定」と表示してよい`
- 対象：`tools/session_logs/read_only_entry.py`の成熟度表示三項目
- 前提となる製品受入判断：`records/development/2026-08-14-stage5-g25-session-artifact-product-entry-acceptance-decision-v1.md`
- 前提SHA-256：`57818c4390c02b866c55708b4292e965144d281a2349a5d12ad27bc4d31b7187`
- 判断：`approved_for_stable_normative_promotion`

## 1. 判断

【記録】利用者は、G25読取り専用入口を最初の製品処理として受け入れた後、正式・安定表示への昇格を
別の判断として許可した。

【判断】次の三項目だけを変更してよい。

- `lifecycle: provisional`（暫定）から`lifecycle: stable`（安定）へ変更する。
- `normative_status: non-normative`（正式基準ではない）から`normative_status: normative`
  （正式基準）へ変更する。
- `promotion_required: true`（正式化に追加承認が必要）から`promotion_required: false`
  （追加承認済み）へ変更する。

## 2. 三案の比較

| 案 | 内容 | 単純さ | 処理時間・メモリ | 頑健さ | 変更範囲 | 保守・戻しやすさ | 判断 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A | 判断記録だけを残し、製品入口の表示を変えない | 最も単純 | 影響なし | 実表示と判断が食い違う | 記録一件 | 戻しやすいが不一致を残す | 不採用 |
| B | 既存の製品入口先頭三行だけを更新する | 単純 | 実行時影響なし | 判断と実表示が直接一致する | コード一件の三行 | 変更箇所が明確で戻しやすい | 採用 |
| C | 成熟度を別の設定fileへ移し、製品入口から参照する | 複雑 | 小さいが読込みが増える | 参照切れの可能性が増える | 設定、コード、試験 | 保守対象が増える | 不採用 |

【判断】案Bは、必要な表示変更を既存の場所だけで完結させる。新しい設定、検査器、試験、台帳を作らず、
処理の入力、出力、安全境界を変えないため、最も小さく十分である。

## 3. 確認方法

成熟度三項目は実行時の振る舞いではなく、製品入口の先頭に置かれた宣言である。現在、この三項目を合否判定
する既存試験はない。今回だけの値を固定する新試験は追加しない。

次を確認する。

1. 変更前後の差分が三行だけである。
2. 変更後のfileを再読込みし、三項目が承認値と一致する。
3. 新入口の対象試験とG25の直接関連試験が成功する。
4. 正規全試験が成功する。
5. 独立レビューで、機能、出力、安全上の限界に変更がないことを確認する。

## 4. 維持する境界

正式・安定表示への昇格は、機能追加ではない。次を変更しない。

- 入力は利用者が許可したローカルSession記録一件だけである。
- 伏字化した会話記録、要約、元記録との対応情報だけを画面へ返す。
- 保存、探索、複数file処理、外部送信、ネットワーク通信、外部処理、Git操作、権限変更を行わない。
- 出力を外部送信許可済みとして扱わない。
- すべての機微情報を必ず検出するとは保証しない。

## 5. 今回の判断に含めないもの

- 第5段を完了とする判断。
- ReviewCompass3自身の作業へ、この入口を直ちに必須適用すること。
- 保存、探索、外部送信などの能力追加。
- G26、G30、他142 path、上流9文書の変更。
