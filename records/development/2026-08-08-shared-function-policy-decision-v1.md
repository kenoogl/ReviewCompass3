# 共通関数化方針 Decision v1（複製禁止・module起動統一）

- decision ID：`DEC-SHARED-FUNCTION-POLICY-001`
- decision maker：Human
- decided at：2026-08-08
- 文言（逐語）：「更に言うと、意図的な複製は禁止した方がよい。共通関数として位置づける。」
  →案イのcode提示に「美しくない」→案ア提示後
  「**案ア＋複製禁止規則のAGENTS明文化＋文書7か所の書き換えを採用**」

## 1. 決定内容

1. **意図的な複製の禁止**：関数・部品の写しを作らない。既存部品はreuse-search台帳で確認し、
   共通部品（`tools/common/`。最小・安定・標準libraryのみ・変更はHuman承認）として一元化する
2. **起動方式の統一（案ア）**：toolsのscriptはrepository根元から`python3 -m`のmodule起動で呼ぶ。
   fileへのpath直接起動を前提にしない（sys.path定型の複製も置かない）
3. 生きている文書のcommand記載**7か所（5文書）**を`-m`形式へ書き換える。歴史recordは書き換えない
4. 本決定は`DEC-EGRESS-B-CHECK-001`の効果基準を上書きしない。根本原因の診断
   （Human 2026-08-08「これはTDDが原因である。…台帳を見て、再発明はしないというルールで
   作成したら、このようにはなっていない」）を前提に、無自覚な再発明と意図的複製の双方を禁じる

## 2. 適用範囲と例外

- digest系10定義を共通化（先行）。B（例外）・D（_within）・E（印字）系統は後続の実施単位で同方式
- **例外**：`todo_snapshot.py`は凍結契約がfile指紋を固定しており、凍結解除のHuman判断まで
  写し1件を残置（残置分は出力一致テストで守る）。解除後1行で追随
- 統合レーン総括（`45438e4c…`）の「分岐検出テストで守る」案は本決定により**上書き**される
