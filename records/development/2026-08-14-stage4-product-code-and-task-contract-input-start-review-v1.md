# 立て直し計画v5 第4段 製品コード候補とTask Contract入力の範囲固定 独立開始前レビュー v1

- レビュー日：2026-08-14
- 状態：`completed`
- 対象作業票：`docs/development/2026-08-14-stage4-product-code-and-task-contract-input-bootstrap-work-ticket-v1.md`
- 対象作業票SHA-256：`26c0b1d117067112881a289fc871a33c374d6693e6c157b2c96c10bb4d5557c8`
- 対象作業票commit：`aa8b7c033c376ab095b5a3792d05160f1b964746`
- 観測commit：`66d608e5b5d605ddaf387bbd75a507ac934800c6`
- レビュー担当：作業担当と異なる新規サブエージェント

## 1. 判定

**開始可**。

【実測】固定材料、コード母集合、関連試験集合は再現できた。作業票は、候補文書を正式な正本へ
昇格せず、用途と今後の扱いを分け、全体へ軽い確認、一候補の到達範囲だけへ詳しい確認を行う。
未完成のTask Contract機構、外部送信、使用停止中のIssue状態更新を実施前提にしていない。

【判断】利用者が作業票§13の四点を承認した後、案Aの読み取り専用方法で開始できる。
本レビューは四点の利用者承認、正式製品コードの採用またはTask Contractの承認を代行しない。

## 2. 固定材料と変更範囲

【実測】作業票一件と、作業票§2が固定する13件のSHA-256を実fileから再計算し、全件一致した。
第3段完了判断のSHA-256も一致した。対象commit `aa8b7c0`の変更は作業票一件の追加だけであり、
観測commitから対象commitまで、`tools/`、`tests/`、`setup.py`、`conftest.py`、`pyproject.toml`、
`config/`に差分はなかった。

固定された正本4件は、立て直し計画、開発方針、第4段の正式製品コード識別追補、第4段の軽量経路と
製品機能を分ける追補である。製品上流文書9件はすべて`lifecycle: provisional`かつ
`promotion_required: true`で、`non-normative`、`review-candidate`、`successor-candidate`または
統合候補であった。配置名の`current`を正式採用の根拠にしていない。

## 3. コード母集合と関連試験集合の独立再生成

成果物の集計を使わず、観測commitの`git ls-tree -r`からpathを抽出し、path昇順の一覧と
`mode type object-id<TAB>path<LF>`を再生成した。

| 集合 | 件数 | path一覧SHA-256 | Git tree entry一覧SHA-256 |
| --- | ---: | --- | --- |
| `tools/**/*.py`、指定した拡張子なしfile、`setup.py` | 152 | `5d116414e108851af39710abc8483c0a5c48bd7b6a9a7db8377d014dc650e3ed` | `c8c1cf64d011ce15234a584b1604953907bd87051673eb74df8475ebcda4c29e` |
| `tests/**/*.py`、`conftest.py` | 192 | `f6db6ef2955bb4dcec171580e01660681b51704af47eaa823ad5f481242a856f` | `3426dbe2529af2f2971eb1d5c7c75678da39726d7d416081aae1131837d1d821` |

【実測】152件の内訳は`tools/**/*.py` 150件、
`tools/deployment/installed/trusted-review-send` 1件、`setup.py` 1件で、申告と一致した。
192件は試験・試験補助集合として別に固定され、製品・開発支援コードの用途分類へ混入していない。

## 4. 母集合漏れへの反証

中心判断「152件が現在treeのPython製品・開発支援コード候補を覆う」を崩すため、次を調べた。

1. 全追跡fileのshebangを検索した。該当は
   `tools/deployment/installed/trusted-review-send`一件だけで、152件へ含まれていた。
2. Git mode `100755`のfile、symlinkの`120000`、submoduleの`160000`を全treeから検索した。
   該当は0件だった。なお上記wrapperのGit modeは`100644`だが、shebang規則で捕捉されている。
3. shell、JavaScript、TypeScript、Ruby、Perl、PowerShell、Go、Rust、Java、C/C++等の追跡済み
   source拡張子と、`tools/`・`tests/`・rootの既知file以外にあるPython fileを検索した。該当は0件だった。
4. `pyproject.toml`と`setup.py`のconsole entry pointを確認した。全targetは152件内のPython moduleだった。
5. 生成source用の追跡directory名と、母集合外の実行source候補を検索した。該当は0件だった。

【実測】反証は不成立で、コード母集合の件数と範囲を維持する。

【実測】拡張子なしwrapperは、現treeに存在しない`tools.api_providers`をimportし、外部送信入口を称している。
これは母集合から除く理由ではない。作業票は同fileを高危険度の確認対象へ含め、最初の候補から除外し、
利用先、参照切れ、使用停止境界を確認するため、既知の不確かさを隠していない。

## 5. 上流候補、Feature、Architecture Policy

【実測】製品上流候補9件には、Intent候補、統合製品・開発計画候補、用語集候補、Task Contract構想候補、
Requirements候補3件、設計差分候補、旧第5段からの継承候補が含まれる。Feature候補は統合計画§3.1と
Requirements候補に、Architecture Policy候補は統合計画§6.2、Task Contract構想、設計差分§3.2に
記述される。`document_role: feature`を持つ独立文書、またはTask ContractのArchitecture Policyとして
採用済みの独立した上流文書は見つからなかった。開発方針や過去のWork 4A用policyは、この製品上流の
Architecture Policyと取り違えない。

【判断】この不在は母集合の欠落ではなく、第4段で有効性と不一致を確認する対象状態である。実施担当は、
候補節を正式なFeatureまたはArchitecture Policyが既に存在する証拠にしてはならず、不在または未採用を
観測として利用者へ戻す必要がある。作業票の停止条件と正式採用境界はこの扱いを支えている。

【実測】統合3文書のfront matterにあるpathとSHA-256の組を全件再計算した。47参照中44件は一致し、
不一致3件は作業票§2.3と同じだった。

1. Intent候補のsource catalog参照：宣言`8564e956…`、実測`1a40adce…`。
2. 統合計画候補の開発方針参照：宣言`d37a60ab…`、実測`422d234a…`。
3. 用語集候補のIntent参照：宣言`307bdbcc…`、実測`1950f5a3…`。

【判断】三件を実施入力として残し、この作業で参照値、候補文書または出所を修正せず、候補を自動失効・
自動採用しない境界は妥当である。報告されていない同型の直接参照不一致は見つからなかった。

## 6. 分類軸、意味群、確認の深さ

【判断】用途の`製品／開発支援／共有／用途不明`と、今後の扱いの
`採用候補／保留／使用停止／履歴のみ`は独立している。共有と用途不明を受け止められるため、
配置名だけによる製品・開発支援の取り違えを避けられる。現在treeに残るfileを`履歴のみ`候補とする場合も、
残存理由を確認するため、過去状態の再構成用途と現在利用を混同しない。

【判断】import、入口、呼出し元、設定、入出力、保存先、試験、根拠から意味群を作り、152件を一度だけ
割り当て、共有群と用途不明群を許す方法は、file一件ずつの採否より上位方針と整合する。

【判断】152件全体への確認は、列挙、参照関係、入口、関連試験、分類と集合照合に限られる。詳しい確認は、
選定基準を満たす利用者向け処理一件から到達する閉包だけである。到達外の全関数へ全文レビュー、変異検査、
削除・統合案を要求しないため、範囲は重要度と不確かさに比例している。

最初の候補は、現在の利用者価値、現存する入口、閉じられる依存、確認できる入出力・失敗、関連試験と
根拠を必要とする。外部送信、不可逆操作、権限変更、使用停止中のIssue状態更新、未完成のTask Contract生成器・
状態機械・許可機構を必須にする処理は除外される。候補の正式採用は利用者判断まで成立しない。

## 7. 三案比較

【判断】案Aは既存Git、`rg`、Python標準の構文木、既存試験・文書参照と、repository外の一時処理だけを使う
最小案である。案Bは対象をprocess呼出しへ限定した既存目録の保証範囲を全コードへ読み替える必要があり、
既知の誤表示がある。案Cは`REQ-WORKFLOW-009`の恒久目録・台帳を前倒しする。

比較は、単純さ、処理時間、メモリ使用量、頑健さ、変更範囲、保守負担、戻しやすさを扱う。
必要な母集合・参照・分類を得ながら恒久物を増やさない案Aの選択理由は具体的である。比較のための新しい
script、台帳、検査器、試験、関門をrepositoryへ作らない条件も明示されている。

## 8. Human境界と停止条件

【判断】開始前に利用者へ戻す事項は次の四点に限定され、十分である。

1. 上流候補、152件、192件の母集合。
2. 用途と今後の扱いの二軸。
3. 最初の候補の選定基準と、詳しい確認を一候補の到達範囲へ限定すること。
4. 案Aの読み取り専用方法。

正式製品コード集合、採用しない集合、最初の製品処理候補、第4段完了は後続の利用者判断であり、
今回の開始承認に含まれない。上流候補の競合、分類不能、高危険度処理への依存、集合の未分類・重複・混入、
範囲外変更、新機構の必要、利用者判断の代行が生じた場合に停止する条件も明示されている。

## 9. 止める指摘と報告不一致

- 止める指摘：0件。
- 報告不一致：0件。

補助的なfront matter読取commandの一回目は、zshの特殊変数`path`をloop変数に使って実行pathを
上書きしたため`sed`を解決できなかった。変数名を`doc_file`へ変え、絶対pathの`/usr/bin/sed`で再実行して
成功した。file変更はなく、上記の判定と集計には失敗commandの出力を使っていない。新機構の追加候補にはしない。

## 10. 未実施

【未実施】152件の意味群分類、用途・今後の扱いの裁定、最初の製品処理候補の選定、到達閉包の詳しい確認、
コード・試験・設定・schema・上流文書・TODO・Issue・既存Evidenceの変更、正式製品コードの採用、
Task Contractまたは`REQ-WORKFLOW-009`の採用・実装、全試験、変異検査、外部送信、第4段完了判断は
行っていない。文書と読み取り調査の開始前レビューであるため、全試験と新しい反証試験は要求していない。

## 11. 次の一作業

本記録の単独commitと作業単位遷移確認後、利用者へ§8の四点を提示する。利用者が承認するまで、
分類Evidenceの作成と最初の候補選定を開始しない。
