# Issue解決処理v4の成熟度精査・限定訂正レビュー v1

- レビュー日：2026-08-14
- 対象訂正commit：`9359485`
- 訂正前commit：`6fafe0b`
- 訂正対象：`records/development/2026-08-14-issue-resolution-v4-maturity-reassessment-evidence-v1.md`
- 訂正後SHA-256：`8038ce27b0c3fa41e0ebdb70a860811d4bb7e1649847b16c0a88c25d5834d050`
- 先行レビュー：`records/development/2026-08-14-issue-resolution-v4-maturity-reassessment-independent-completion-review-v1.md`
- 先行レビューSHA-256：`893fde2d1d05f438f47b87fe28ac5c5103081ac0eec127019a14f58c7b7aa1fd`
- 確認範囲：先行指摘`IR-MAT-REV-001`に対する訂正だけ

## 1. 判定

【判断】`verified`。

【判断】先行指摘`IR-MAT-REV-001`は解消した。訂正後Evidenceの原因説明は実コードと一致し、既存試験との差を
正しく区別している。三案は七観点を維持し、観測した欠陥だけを直す最小案として案Cを選ぶ理由も成立する。

【判断】中心判定「`修正後に再判定`」は維持される。これはコード修正の開始許可、正式利用可能の判定、現役化、
対象Issueの状態反映を意味しない。

## 2. 限定差分

【実測】訂正commit `9359485`の親は`6fafe0b`である。親からの変更は対象Evidence一件だけで、14行追加、9行削除だった。
変更箇所は§6の原因説明と、§7の三案比較・選択理由だけである。`git diff --check`は終了コード0だった。

## 3. 先行指摘の解消確認

### 3.1 原因説明

【実測】`resolve_issue`の構文木を機械確認した。`record_file.parent.mkdir(...)`は255行目にあり、254行目から始まる
`try`の内側だった。この`try`の捕捉対象は`ResolutionError`だけである。`Path.mkdir`が直接出す`OSError`は捕捉されず、
Issueを元の内容へ戻す処理へ入らない。

【判断】訂正後の原因説明はこの構造と一致する。「`mkdir`が`try`の外にある」という先行の誤説明は残っていない。

### 3.2 既存の部分書込み試験との差

【実測】`test_record_write_failure_restores_issue_without_partial_record`は、裁定記録とEvidenceの準備によって
`records/development/`を先に作成した後、同フォルダ配下の`Path.write_bytes`へ部分書込み失敗を注入する。
したがって、実行時の`mkdir(..., exist_ok=True)`は既存フォルダを通過し、故障注入点にはならない。

【実測】注入された`OSError`は`_atomic_write`が`ResolutionError`へ変換するため、既存の`except ResolutionError`に
捕捉される。一方、今回の反証はその一段前の`mkdir`自身が生じさせる`OSError`である。

【判断】訂正後Evidenceは、既存試験が書込み失敗後の復元を検査することと、親フォルダ作成失敗を検査しないことを
正しく区別している。

### 3.3 三案比較

【実測】三案の表には、単純さ、処理時間、メモリ使用量、頑健さ、変更範囲、保守負担、戻しやすさの七観点が残る。

【判断】案Aは新しい親フォルダを作れる現行能力を狭める。案Bは処理位置の移動と失敗変換を要する。案Cは現在の
処理順と既存の復元構造を保ち、捕捉対象へ`OSError`を加える限定変更で、観測した片残りを防げる。案Bより復元依存は
残るが、案Bと案Cのどちらも今回の欠陥を防ぐという比較範囲では、単純さ、変更範囲、保守負担、戻しやすさから
案Cを最小案とする理由は成立する。

### 3.4 中心判定と訂正外のEvidence

【実測】訂正前後の差分に、§1の「`修正後に再判定`」、正式利用を止める判断、反証結果、正常例、二つの拒否境界、
手戻り、未実施事項の変更はない。訂正は中心反証を取り消さず、原因と修正候補の境界を狭めている。

【判断】訂正外のEvidenceを古いものとして扱う必要はない。中心判定も変更されていない。

### 3.5 禁止対象の不変

【実測】`9359485^..9359485`で、コード、試験、設定、対象Issue、TODO、第4段計画、第4段の二つの追補判断の差分は
0件だった。対象コード、対象試験、設定、対象IssueのSHA-256も先行レビューの固定値と一致し、対象Issueは
`registered`のままである。

## 4. 止める指摘

【判断】0件。

## 5. 報告不一致

【判断】0件。

## 6. 試した反証

1. 【実測】訂正後も`mkdir`を囲む`try`が`OSError`を捕捉している可能性を構文木で確認した。捕捉対象は
   `ResolutionError`だけであり、訂正後説明への反証は不成立だった。
2. 【実測】既存の部分書込み試験が親フォルダ作成失敗も含む可能性を、準備処理、注入先、`_atomic_write`の
   例外変換から確認した。親フォルダは事前に存在し、注入先は`write_bytes`であるため、反証は不成立だった。
3. 【実測】限定訂正に禁止対象が混入した可能性を親commitとの差分で確認した。変更はEvidence一件だけだった。

## 7. 未実施

【未実施】既確認事項の全体再レビュー、既存試験と全試験の再実行、故障注入の再実行、コード・試験・設定・Issue・
TODO・第4段資料の変更、コード修正、正式利用可能の裁定、現役化、状態反映、新しい機構・検査器・試験・関門、
履歴書換え、外部送信は行っていない。
