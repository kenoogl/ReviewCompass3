# 機微検査精密化・改名 実装の起草側自己レビューと文脈整理 v1

- 実施日：2026-08-16
- 担当：Claude（実装担当による起草側の事前点検。独立完了レビューの代替ではない）
- 対象：GREEN commit `0665120`の実装（改名済み送信路・除外3形式・出し分け・対象試験61件）と、
  独立対処commit `3e9a9f7`（layout検査の応答raw除外）
- 方法：契約v2 §7〜§10を実装・試験と突き合わせ、誤合格・未接続・禁止作用・上位悪影響の4類型で点検

## 1. 発見

なし（開示事項も含めて0件）。契約定義段階の自己レビュー（SR-C9-1〜3）で内部矛盾を先に解消したため、
実装は契約の写像に収まった。

## 2. 契約対応の確認（実測点検）

1. **定数の閉じ込め**：除外3形式は`_HIGH_ENTROPY_ALLOW_PATTERNS`として`send.py`へ直書き。設定file・
   環境変数・引数・送信指示から変更する経路はない。
2. **出し分けの一意性**：`_scan_order`のJSON走査で`path == ("order_identifier",)`のときだけ
   `allow_high_entropy_exclusions=False`。dict key名（`path=None`）と他fieldは除外適用。由来file内容
   （`_build_payload`経由）は既定値で除外適用。
3. **5 patternの不変**：`_scan_text`冒頭の既定5 pattern検査は引数に関わらず無条件で実行される。
4. **redaction.py無変更**：公開引数`allow_patterns`の利用のみ（保護対象の差分0を機械確認済み）。
5. **試験の検出力**：RED 3件（精密化がないと失敗）＋敵対9件（乱雑列6種・39/41/63/65桁境界hex・
   purpose乱雑列）＋既存49件（64hex識別子停止`test_random_identifier_stops_as_specified`を含む）。
6. **改名の完全性**：コード・設定・試験から旧名残存0（全文検索）。実行名は
   `reviewcompass3-external-review-send`。

## 3. 限界の明示（レビュアへ伝える）

- 残余risk 3点（40/64桁hex実鍵・大文字乱雑連結・UUID従来対象外）は契約§7.2が明示し、独立確認で
  受容妥当と判定済み。実装はこの契約どおりであり、riskを追加も縮小もしていない。
- layout検査の応答raw除外（独立対処）は契約009の範囲外の別件で、利用者承認の独立commitである。
  除外は台帳の固定名`--response-v1.raw`だけに限り、他fileの検知は試験で維持を固定した。

## 4. 文脈整理（依頼promptへ含める判断済み事項・範囲外）

- **判断済み（蒸し返し不要）**：除外3形式と適用範囲（契約v2 §7・独立確認`開始可`）。残余risk 3点の
  暫定受容（採用判断）。改名の同梱（利用者指示）。layout除外の対処方針（利用者承認(1)）。
- **範囲外（「無い」という指摘は不要）**：応答解析・監査自動化・旧設計統合・複数送信・依頼組み立て器・
  prompt品質gate・判定取り込み（後続）。歴史的recordの旧名書き換え。実送信E2E（受入条件8・利用者指示待ち）。
- **レビュアへ特に依頼する深掘り**：(1)出し分けの実装が契約§7.2と完全に一致するか（適用すべき場所への
  適用漏れ・適用してはならない場所への適用）、(2)精密化12試験が§7を固定する検出力を持つか、
  (3)layout除外が境界検査の趣旨（自作成果物への絶対path混入禁止）を損なわないか。
