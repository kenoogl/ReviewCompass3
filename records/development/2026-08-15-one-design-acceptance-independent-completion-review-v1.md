# 一件の設計・受入条件照合 独立完了確認 v1

- 実施日：2026-08-15
- 対象commit：`fb8c0b6443a0491ea635dbc9e8c3981e2df6fee8`
- 実施者：実装担当と別のAI実行単位
- 実施方法：成果物変更なし、外部送信なし、読取り専用
- 判定：`correction_required_before_product_acceptance`
- 止める原因系統：1
- 欠陥変種：3
- 不成立条件：9、12
- 条件18：修正要のため未達
- Human条件20：未判断

## 判定

【判断】入力形式検査に、形式版の型誤受理と、正準JSON化できない入力を固定形式停止へ変換できない同一系統の欠陥がある。条件9と12を満たさないため、製品受入判断へ進めない。

## 機械反例

### 1. schema_versionの型誤受理

【実測】設計側と受入条件側それぞれの`schema_version`へ`true`または`1.0`を入れた4例がすべて成功した。Pythonの`True == 1`と`1.0 == 1`を現在の比較が受け入れるためである。

【判断】固定形式を誤って成功させ、条件9に不適合である。

### 2. 巨大整数の生例外

【実測】比較値へ5,000桁整数を入れるとJSON読取りから生の`ValueError`が出た。正式入口では終了コード4、`internal_failure / none`となった。

【判断】期待する終了コード2、`invalid_schema / design`または`acceptance`へ変換できず、条件9と12に不適合である。

### 3. 単独surrogateの生例外

【実測】JSON escapeの単独`surrogate`を比較文字列へ入れると、内容識別値の正準UTF-8化で生の`UnicodeEncodeError`が出た。正式入口では終了コード4、`internal_failure / none`となった。

【判断】正準JSON化できない文字列を入力検査で拒否できず、条件9と12に不適合である。

## 条件1〜19

- 成立：1〜8、10〜17、19
- 不成立：9、12
- 未達：18。本独立確認が修正要のため
- Human条件20：本確認の対象外

【実測】JSON同名項目、型付き比較、順序正規化、通常のSHA-256連鎖、`/`からの全要素非追跡、hard link、読取り前後のsize・機器番号・inode、source別の既存停止、安全表示、配置後repository外実行は成立した。

【実測】正常読取り100回とopen失敗100回のdescriptor反復確認で、開いたまま残るdescriptor増加は0だった。製品2fileから通信、外部process、環境値、Git、書込み・削除操作は検出されなかった。

## Test、禁止作用、退行

【実測】

- 対象91件：終了コード0
- 既存G08関連31件：終了コード0
- 既存G08保護4file：基準commit `40b399d`から差分0
- 境界4 RED後の試験差分：0
- `git diff --check`：終了コード0
- 配置後正式名のrepository外実行：終了コード0、stderr空
- 外部送信、成果物変更、Git変更：0
- 固定commitとの対象差分：0
- 最終worktree：clean

【実測】通常hostの既存executor試験は、`ANTHROPIC_API_KEY`の存在を既存安全機構が拒否し、12件失敗・16件成功、終了コード1だった。禁止認証環境名6件を除くと同じ28件が成功、終了コード0だった。

【判断】completion Evidenceの「通常hostの12失敗はG08退行ではなく、隔離全2,111件成功が有効」という判断は妥当である。ただし今回の3反例は既存試験に未接続だったため、2,111件合格だけでは誤合格を防げなかった。

## 限定修正

同じ形式検査系統を一周で閉じる。

1. 設計・受入条件の`schema_version`を厳密な整数型1だけにする。
2. 設計・受入条件それぞれで真偽値と浮動小数点1.0を拒否する先行失敗試験を追加する。
3. JSON読取り時の整数桁数超過を対象sourceの`invalid_schema`へ変換する。
4. 全比較文字列と文字列配列要素で、正準UTF-8化できない値を対象sourceの`invalid_schema`で拒否する。
5. 巨大整数と単独surrogateを設計側・受入条件側の両方で試験する。
6. 対象、関連、隔離全試験、独立完了確認を再実行する。

## 手戻り

【実測】独立反例の補助コードは、初回に正規化後配列の位置を誤指定して`AttributeError`、最終差分確認では基準commit文字列を一度誤記して終了コード128となった。対象をsubjectで選ぶ形と正しい固定commitへ直して各終了コード0で再実行した。製品成果物への影響はない。

## 結論

【判断】固定commit `fb8c0b6`は製品受入判断へ進めない。上記3変種を先行失敗試験へ固定して同一原因だけを限定修正し、再独立確認がverifiedになるまでHuman条件20を求めない。
