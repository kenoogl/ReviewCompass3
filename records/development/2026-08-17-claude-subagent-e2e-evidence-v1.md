# 契約012 実運用E2E（§9-8）Evidence v1

- 記録日：2026-08-17
- 記録者：Claude
- 実施根拠：利用者の明示指示「§9-8実E2Eを実施する。Tier 3の受容を明示する——--accept-tier 3でよい。
  受容根拠record→別名依頼record→起動e2e-012-001まで進めて」→blocking所見の報告後「推奨案を実行」
  （＝F-1〜F-3修正→依頼record再生成→e2e-012-002で再起動。いずれも2026-08-17 chat）
- 結果：**§9-8（実運用E2E）が成立**。2往復目`e2e-012-002`の判定は`verified_with_findings`
  （blocking 0件・非blocking 2件・unexamined明示）

## 1. 前提の解消（実測起点の訂正3件・pin更新）【記録】

E2E前の実測で、契約どおりの起動が3点で不成立と判明し、いずれも利用者承認の訂正recordで解消した：
`--verbose`列挙漏れ（CLI必須旗）、通過変数の`USER`欠落（保存済みsubscriptionログインの読み出し
必要条件。実行器9変数と同値化）、抑制注入変数9種の流用（改善候補採用。自動更新抑止等）。付随して
claude本体の自動更新（2.1.220→2.1.224）による`claude_bootstrap`のpin不一致24件を、利用者承認の
pin更新で復旧した（各recordはTODOのEvidence一覧参照）。

## 2. 一往復目（e2e-012-001）【実測】

- 受容根拠record（Tier 3明示受容）→契約011正式経路で別名依頼record（slug末尾`-subagent`・
  digest表12行・基準commit `6b2eacb`）を組み立て。機微検査が先頭記号つき長識別子2つを検出して
  一度停止し、意味不変の表現へ修正して**check全項目合格**（commit `86af2fb`）。
- 起動`--accept-tier 3`→レビュー実行（`claude-opus-5`・読み取り専用・抑制注入つき）→応答model照合→
  判定JSON抽出→schema検証→判定record機械転記・単独commit（`6336403`）→事後照合4点合格。
  **経路は全自動で完走**。
- 判定は`rejected`：**blocking所見F-1**（agy経路の応答model照合が和集合を使用。model定数固定の
  時点からagyの実効許可modelが2値へ拡大——契約§7.1「agy値は移設のみ」の破れ。起草側のコード
  再読でも実在を確認）。非blocking 6件（F-2〜F-7）。Tier 3・受容根拠は起動record・判定recordへ
  刻印された。

## 3. 修正（利用者採用）【実測】

- RED先行：F-2の固定試験`test_agy_model_check_uses_agy_list_not_union`（和集合にだけ入るmodel名を
  agy起動へ流し停止を要求）を先に追加し、修正前実装で失敗を機械実証。
- 修正：agy分岐の照合をagy専用一覧へ1行差し替え（契約根拠のコメント付き）。試験側の和集合
  差し替え4箇所（起動補助1・個別試験3）をagy専用定数へ追随（検査意味は不変）。F-3対処として
  claude経路の判定取り込み停止試験2件（JSON抽出不能・schema必須鍵欠落→
  `verdict_schema_nonconforming`・未加工出力保存済み）を追加（既存挙動の固定・追加時点から緑）。
- 検証：契約012対象60件・関連161件・正規全試験2,467件 各単独終了コード0。commit `b55903d`。

## 4. 二往復目（e2e-012-002）【実測】

- 受容根拠record（「推奨案を実行」の承認文言・前回受容の引き継ぎ）→修正後状態で別名依頼を再生成
  （digest表13行。**前回判定recordを対象へ追加**・基準commit `b55903d`）。check・起動前検査とも
  一発合格（commit `2732ecd`）。
- 起動→判定：**`verified_with_findings`・blocking 0件**。反証点4件（F-1修正の実効・修正の回帰・
  F-3対処の実効・前回成立事項の維持）すべて「反証できず」。非blockingはR-1（schema不適合側試験に
  raw保存確認が無い・実害なし）とR-2（和集合が組み立て器照合で2値になる構造情報・契約§5.1-4の
  設計どおり）の2件。判定record機械転記・単独commit（`63bb1dc`）・事後照合4点合格。

## 5. 受入条件の充足状況と仕分け

- §9-1〜7：実装Evidence（2026-08-17）＋訂正record3件＋本record §3（REDの機械実証）で充足。
- §9-8：**成立**（2往復。Tier 3の宣言＋明示受容・受容根拠record・別名依頼・機械転記・事後照合の
  全経路を実測。1往復目が全2,464試験で捕まらなかった契約違反を検出し、2往復目が修正を独立確認——
  第2 oracleの実効を実証）。
- §9-9：正規全試験2,467件成功・終了コード0（F-1〜F-3修正後）。
- §9-10：未実施（agy・Tier 1。E2Eと同一対象集合13行で2 oracle比較を成立させる）。
- §9-11：未実施（§7.4残余risk 4点の利用者受入判断）。
- 仕分け待ち：F-4〜F-7・R-1〜R-2（非blocking。判定record 2件が正本。TODOのHuman判断待ちへ登録済み）。
