# 第5段 G25 Session成果物入口 正式・安定表示への昇格 独立完了レビュー v1

- レビュー日：2026-08-14
- 対象判断：`records/development/2026-08-14-stage5-g25-session-artifact-maturity-promotion-decision-v1.md`
- 対象判断SHA-256：`b0529f44d202c4b9c49600624417a54a611ad8eb77581ee9515c941291f850d1`
- 前提となる製品受入判断SHA-256：`57818c4390c02b866c55708b4292e965144d281a2349a5d12ad27bc4d31b7187`
- 変更commit：`f2bf9a70daea94d0ff2b2c3aed6a4e697a3cef4a`
- 対象Evidence：`records/development/2026-08-14-stage5-g25-session-artifact-maturity-promotion-evidence-v1.md`
- 対象Evidence SHA-256：`51df5b3b84ce3ca846fc7206b0c1c9ad290db6021bb0dbe91f5f2dd4297bd6a4`
- 観測commit：`4881cc4`
- レビュー担当：新規の独立担当

## 1. 判定

【判断】**verified**。正式・安定表示への変更は利用者判断と一致し、製品入口の機能、安全境界、外部送信未承認の
扱いを変えていない。第5段完了や自己適用の必須化も先取りしていない。

【判断】止める指摘は0件、報告不一致は0件である。

## 2. 開発物の機能と用途

この製品入口は、利用者が許可した一件のローカルSession記録を読み、機微情報を伏せた会話記録、要約、元記録との
対応情報を画面へ一回返す。過去作業の確認、引継ぎ、レビュー、調査に使う。

入力は許可された記録の場所であり、出力は安全な項目だけから成るJSON一件である。保存、探索、複数file処理、
外部送信、ネットワーク通信、外部処理、Git操作、権限変更は行わない。出力は外部送信許可済みにならず、すべての
機微情報を必ず検出できるとも保証しない。

## 3. 変更範囲と利用者判断との一致

【実測】判断記録、製品受入判断、EvidenceのSHA-256は申告値と全件一致し、固定commitはすべて実在した。

【実測】変更commitの差分は`tools/session_logs/read_only_entry.py`一件の先頭三行だけである。

- `lifecycle: stable`
- `normative_status: normative`
- `promotion_required: false`

【判断】三項目は、利用者の文言「この入口を『正式・安定』と表示してよい」と一致する。引数、入力処理、三形式の
解析、伏字化、要約、来歴、安全な出力選択、絶対path検査、停止結果、終了コード、配布入口に差分はない。
試験、設定、`pyproject.toml`にも差分はない。

## 4. 機能不変の反証

【実測】中心判断「成熟度表示だけが変わり、利用者が受け取る結果は変わらない」を崩すため、変更前commitと
変更後commitを別々に展開し、同じ合成Claude記録を両方へ入力した。双方とも終了コード0で、標準出力と標準エラーは
バイト単位で一致した。標準出力のSHA-256は
`67b7b155c5622fd740f35a10d7df15fc33bf4a7dfe26626ee25a857c09f6fcc4`だった。emailは伏字化され、
`external_send_approved: false`も維持された。反証は成立しなかった。

【判断】今回の値だけを固定する新試験を追加しなかった判断は妥当である。変更対象は実行時の分岐ではない三行の
宣言であり、直接再読込み、差分確認、既存の対象・関連・全試験、上記の変更前後比較で確認できる。新しい試験を
作ると、この一回の状態値だけを保守する対象が増えるため、本変更には過剰である。

## 5. 試験と受領記録

【実測】対象Evidenceが示すリポジトリ外の受領記録は実在し、SHA-256は
`ac13868e527b57fa439d7c79bdb8c1e260271771a37820f4091c61e9bdad7b97`で一致した。記録内容は、正規全試験
1,740件成功、失敗0、error 0、skip 0、終了コード0、Python 3.13.14、pytest 8.4.2、runner版2、
代替実行なしである。

【実測】現在の作業ツリーから対象Evidence一件だけを除き、正規処理と同じ規則で状態識別値を再計算した。結果は
`16ea8aa7023f82decc21d7177a0d146fe914eb019541a396fa9f1f81ffa719f2`で、対象受領記録と一致した。
この一致が結び付けるのは、Evidence作成前で昇格commit後のローカル作業状態であり、Git commitの木そのものと
取り違えない。

【実測】変更commitの履歴付き複製でも独立再実行した。対象試験は12件成功、G25直接関連14 fileは55件成功、
正規全試験は1,740件成功、失敗・error・skip 0、終了コード0だった。独立受領記録の状態識別値は
`054d36f3b9e1dc8372516b5591bab0781cf516d86daf8b74ce4790a0369fbec5`であり、履歴付き複製の変更commit状態に
一致した。これにより、対象Evidenceのローカル状態識別値だけに依存せず、変更commit自体でも成功を確認した。

## 6. 手戻り記録の確認

【実測】対象Evidenceが記す最初の全試験起動の誤りは、正規処理が受け付けない`--receipt-output`を指定したことで
あり、試験開始前に終了コード2で停止している。正しい既存引数は`--receipt`である。その後の単独再実行は上記の
1,740件成功と受領記録を残している。

【判断】この誤りは操縦時の引数名の取り違えであり、製品コード、試験結果、正式・安定表示の妥当性へ影響しない。
経緯と原因を隠さず記録し、一度の誤記のための新しい仕組みや試験を追加していない扱いも妥当である。

【実測】本独立レビューでも、Git履歴を含まない展開物で正規全試験を起動したため、履歴を必要とする25件が環境理由で
失敗した。期待した実行役と実際の実行役は同じ正規処理であり、原因はレビュー用の展開物にGit履歴が無かったことである。
履歴付き複製へ切り替えて同じ正規処理を再実行し、1,740件成功を確認した。製品または試験の欠陥ではなく、今回の
レビュー内で閉じたため、新しい機構や追加作業へ回さない。

## 7. 先取りしていない範囲

【実測】昇格判断とEvidenceは、第5段完了、G26、G30、他142 path、上流9文書、自己適用の必須化を今回の判断から
明示的に除外している。変更commitにもこれらの変更はない。

【判断】`stable`（安定）と`normative`（正式基準）の表示は、この製品入口が受入済みであることだけを示す。
ReviewCompass3自身の全作業へ直ちに使う義務や、第5段全体の完了を意味しない。

## 8. 止める指摘と報告不一致

- 止める指摘：0件。
- 報告不一致：0件。

## 9. 未実施

【未実施】製品コード、試験、設定、TODO、Task Contract、既存Decision、既存Evidenceは変更していない。第5段完了、
自己適用の必須化、G26、G30、他142 path、上流9文書、保存、探索、外部送信、権限変更は判断も実施もしていない。
新しい試験、設定、検査器、台帳、関門は追加していない。実Session記録は使用せず、反証には合成入力だけを使った。
