# 判定方式の検証 経緯と結論 Decision v1

- decision ID：`DEC-EGRESS-METHOD-CONCLUSION-001`
- decision maker：Human
- decided at：2026-08-08
- 決定文言（逐語）：「外部API比較はしない。統合へ進む」

## 1. 目的（何のための実験だったか）

Human設定（2026-08-07「外部API比較の意味は、サブエージェント方式で代替できるかが見たいところ。
その意味では、全データに対して行いたい」）：外部LLM APIへコード断片を送って判定させる方式を、
**新しい外部経路を開かないサブエージェント方式で代替できるか**の検証。代替可能なら、
場面1（部品の実装同一性判定）のために出口を開く必要がなくなる。

## 2. 経緯の連鎖（各段はrecordに固定済み）

| 段 | 内容 | record（SHA-256） |
| --- | --- | --- |
| 前提 | dry-runで曖昧59組のpayload一式を固定（一覧digest `baa6491f…`） | `2026-08-07-egress-dry-run-v1/manifest.json` |
| A1 | サブエージェント4体が59組を判定（緩い基準のみ） | `2026-08-08-egress-a1-subagent-judgment-v1.md`（`2f8dda0d…`） |
| B | Human答え合わせ。一致85%。不一致9組は全て基準未定義が原因。基準4件を確定 | `2026-08-08-egress-b-check-decision-v1.md`（`40530e41…`） |
| A1' | 確定基準v2を明示して再判定。一致92%。残る不一致は単一論点に集中 | `2026-08-08-egress-a1prime-comparison-v1.md`（`18aff58e…`） |
| 裁定 | Human「案イ」で基準v3確定。明確化後の再判定で**一致100%（59/59）** | `2026-08-08-egress-name-contract-adjudication-v1.md`（`bba8c1ac…`） |

## 3. 結論

1. **サブエージェント方式は、この用途において外部API方式を代替できる**。精度の推移
   85%→92%→100%が示すとおり、精度を決めるのは基準の精密さであってモデル系列ではない
   （コードの誤読は通算0件）。外部APIに残る固有の価値は系列独立性のみで、精度上の根拠はない
2. **A2（外部API比較）は行わない**（Human決定）
3. **統合へ進む**（Human決定）。対象はsame 48組が束なる逐語重複6系統。次作業は
   評価②（文脈依存の統合可否）の定義と進め方の提案起草

## 4. 出口機構（段階1・2実装）の扱い

- `tools/egress/`（関門・事前分類・payload・承認検証・dry-run。テスト65件）と提案v4
  （`3a82b397…`）は**完成済みの資産として保持**する。場面1のための段階3・4（許可証正式
  schema化・送信実装）は**着手しない**（必要が再発したときの再判断材料はすべて固定済み）
- 機密関連の実施順序（`DEC-CONFIDENTIALITY-WORK-ORDER-001`）の残項目（規則の登録、
  C・Dの定義、既存データの扱い）は本決定で変更されない。着手はHuman判断のまま

## 5. 境界

- 統合の実施可否そのもの（評価②の各判断）は本決定に含まれない。系統ごとにHumanが判断する
