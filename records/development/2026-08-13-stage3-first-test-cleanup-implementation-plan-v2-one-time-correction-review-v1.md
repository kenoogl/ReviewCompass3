# 第3段 最初の試験整理 実施計画 v2 一回限り修正後確認

- 確認日：2026-08-13
- 状態：`verified`
- 対象commit：`29bcd0ea05df3f0682541207113b58765542705b`
- 対象：`docs/development/2026-08-13-stage3-first-test-cleanup-implementation-plan-v2.md`
- 対象SHA-256：`c470da1e4ed3b19c548b64db0d817bdec2d1236b747d3388f50eeccf8c6d1147`
- 先行レビュー：`records/development/2026-08-13-stage3-first-multi-group-test-cleanup-implementation-plan-independent-review-v1.md`
- 先行レビューSHA-256：`f9966ff0135be6cd01c128d60ffec96218000d0ede4722a838b79f2690115f51`
- 確認担当：v1を確認した同じ独立レビュー担当

## 1. 判定

【判断】`verified`。v1の止める指摘だったG11三試験の現行`TRACEABILITY`接続をv2は明記し、三試験、
専用補助処理、`tests/test_pilot_collaboration.py`をすべて変更対象外へ戻した。採用案はG04の同じ試験fileにある
役割終了二件だけへ限定され、件数、専用定数、Human承認境界も限定範囲と一致する。実施前のClaude確認と
Human承認へ渡せる。

## 2. 固定対象と限定修正

【実測】対象commitは実在し、対象文書のSHA-256は申告値と一致した。対象commitの変更はv2文書一件の追加だけで、
基準commit `b15be8a63acab71378263fdda1600f35c3839aa0`から対象commitまで試験ファイルの変更はない。

【実測】v2が固定するv1、v1独立レビュー、G04役割分類v2、その限定修正後確認、手動確認回数Decisionの
SHA-256は全件一致した。v1→v2の差分は、先行指摘の説明、G11の除外、G04二件への変更範囲縮小、件数と
Human判断の縮小に閉じている。新しい実装、検査器、台帳、試験の提案は追加されていない。

## 3. G11の変更対象外への回復

【実測】v2は、G11三試験、`tests/test_pilot_collaboration.py`の`TRACEABILITY`、G11専用補助処理を
「変更しないもの」へ明示した。v1で列挙した三試験名と補助要素名は、v2の変更範囲、実施順序、確認方法、
Human承認対象に残っていない。

【判断】v2はG11の先行抽出Evidenceにある`役割終了`を今回採用せず、現行要求との接続を廃止・置換する
Human判断がない限り現在保証として維持する。これはv1独立レビューの止める指摘を、その指摘以外へ広げず
解消する扱いである。

## 4. G04二試験と専用定数

【記録】G04役割分類v2は、`test_declaration_map_keys_equal_scope_requirement_ids`を2026-08-11時点の
RED対応表だけを確認する役割終了候補、`test_red_evidence_keeps_green_fields_explicitly_unimplemented`を
RED証跡三文言だけを固定する役割終了・削除判断済みと分類する。v2の削除対象二件はこの分類と一致する。

【実測】現在の`tests/test_claude_bootstrap_entrypoints.py`を構文木で確認すると、`MAP_PATH`の読取りは
一番目の削除対象試験一件だけ、`REQUIREMENT_IDS`の読取りも同試験だけに閉じる。二試験と二定数を
メモリ上で除いた構文木に両定数の未解決参照は残らない。`MANIFEST_ROOT`は残る`BASELINE_PATH`を通じて
現在保証試験が使うため、維持する計画も正しい。

【実測】二つの削除予定試験名を全treeで検索した。実行される試験コードでの利用は各定義自身だけで、他の出現は
履歴資料、候補一覧、計画・レビュー記録だった。G11で見つかった現行`TRACEABILITY`と同型の参照はない。

## 5. 件数とHuman承認

【実測】対象fileの現在の試験関数は8件で、削除対象はそのうち2件、残る試験は6件である。直近の1,739件を
固定したcommitから対象commitまで試験ファイルの差分はない。二件はいずれもparameter展開のない一試験関数なので、
削除後の収集件数は1,737件となる。

【判断】v2は対象file六件成功、全体1,737件成功を実施時の停止条件とし、収集件数だけで完了させず、
各単独commandの終了コードを要求している。v1で起きた件数と合否の取り違えを繰り返さない境界になっている。

【実測】既に削除承認済みのRED証跡三文言試験と、今回新たに承認を求める宣言対応表試験一件が分けられている。
Humanへ返す追加の保証廃止は一試験だけで、変更範囲も一試験file、二試験、専用定数二件へ縮小された。

## 6. 止める指摘と報告不一致

【判断】止める指摘は0件。報告不一致は0件。先行指摘に影響した変更対象、現在保証、試験件数、Human承認対象は
すべて限定修正され、v1で問題のなかったG04履歴資料を変更しない判断と三案比較は維持されている。

## 7. 試した反証

1. 【実測】G11の三試験・補助処理・`TRACEABILITY`が別の変更節に残っていないか、v2全文を名前検索した。
   削除対象や変更pathへの残存はなく、変更しない現在保証としてだけ残るため反証不成立だった。
2. 【実測】G04二試験を消すと別の現行試験が未定義参照になる可能性を全tree検索と構文木で確認した。
   現行コードからの参照は各定義自身に閉じ、反証不成立だった。
3. 【実測】`MAP_PATH`と`REQUIREMENT_IDS`に残る利用者がいる可能性を構文木のLoad箇所で確認した。
   利用は一番目の削除対象試験だけで、反証不成立だった。
4. 【実測】1,739件の基準後に試験が変化した可能性をGit差分で確認した。試験差分は0件で、対象fileの
   8件から二件を除く独立集計も6件となり、計数への反証は不成立だった。
5. 【実測】v1指摘と無関係な変更が混入した可能性をv1→v2差分で確認した。変更はG11除外とG04二件への
   範囲縮小、それに伴う件数・承認・確認方法の更新であり、別の整理候補や新機構への拡張はなかった。

## 8. 未実施

【未実施】対象計画、v1、先行レビュー、試験、専用定数、製品コード、設定、宣言対応表、証跡、TODOの変更、
試験削除、試験実行、全試験、Claude確認、外部送信、push、履歴書換え、第3段完了判断は行っていない。
読み取り、Digest計算、Git差分、名前検索、構文木による集合・参照照合だけを行った。
