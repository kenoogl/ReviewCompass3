# 契約013 実装・実運用E2E（§9-1〜6）Evidence v1

- 記録日：2026-08-17
- 記録者：Claude
- 実施根拠：利用者の採用指示「推奨案で進める」（候補v3採用・実装開始）、E2E指示「案1で進めて。
  組み立て→check→commitのうえ、そのまま起動して（run-id e2e-013-001）」、所見採用指示
  「所見どおり原則record §4を追記更新し、E2E Evidence作成→§9-7完了レビュー起動まで進めて」
  （いずれも2026-08-17 chat）
- 結果：**§9-1〜6が充足**。E2E判定は`verified_with_findings`（blocking 0件）で、自由文類型の
  実用価値（参照文書の陳腐化検出）を初回で実証

## 1. 実装（§9-1〜4）【実測】

- RED先行：新試験8件を先に固定し、7件が修正前実装で期待どおり失敗（`free_text`語彙不在・類型推定の
  本文label誤判定）することを機械実証。golden固定試験（既存2類型の生成結果を正規化してSHA-256を
  試験定数へ固定。実装前実測の機械転記）は追加時点から緑＝byte不変の錨。
- 最小実装：類型登録（語彙・label）・§3の類型分岐（既存2類型は従来の反証点節をbyte不変で維持、
  `free_text`は「依頼内容」自由記入節）・**類型推定の正準位置化**（冒頭「レビュー種別」行だけを正と
  する。SR-C13-1——既存2類型の推定も本文label混入に騙されなくなった）・必須節の類型分岐
  （SR-C13-2）・自由記入節の検査（固定説明文を除いた非空・placeholder不在・fence外digest行の拒否）。
  途中、雛形の固定説明文が記入内容として数えられ非空検査が素通りする欠陥をREDで検出し、固定説明文の
  除外で是正した。
- 入口文書`docs/development/prompts/request-builder-run.md`へ3類型化と使い分け規律（§7.2）を追記。
- 検証：契約013対象40件（既存32無変更＋新設8）・契約012対象67件・G30 75件・layout 13件・
  bootstrap 41件——各単独終了コード0。実装commit `12abe9c`。
- 付随：TODO_NEXT_SESSION.mdが機械上限12,288 bytesを超過して正規全試験3件が失敗していたことを検出し、
  TODO更新手順書（単一入口検証）どおり圧縮・検証`passed`で復旧（commit `7e5635b`）。復旧後の
  **正規全試験2,482件成功・終了コード0**（§9-6）。

## 2. 実運用E2E（§9-5・e2e-013-001）【実測】

- 依頼：`--type free_text`で組み立て（digest表4行＝原則参照record＋実装3 file）。依頼内容＝
  原則record §4「RC3での体現」の主張を現行実装へ照らす整合検査（実在・古さ・誇張の3点）。
  **checkの正準位置推定が本番で初めて働き`request_type: "free_text"`を正しく判別**
  （commit `e486f3a`）。
- 起動→判定：agy（Tier 1・`gemini-3.1-pro-high`）で一往復完走。判定
  **`verified_with_findings`・blocking 0件**。体現主張は全て実在・記載どおり、誇張なし。
  所見`SEC4-OUTDATED-FREE-TEXT`（minor）＝§4の原則1・2・8の記載が契約013実装を未反映で古い
  （行番号つき根拠）。未検査の正直な申告あり（原則8のfixture実在は試験fileが対象外・digest機械計算
  不可）。判定record機械転記・単独commit（`f9121b2`）・事後照合4点合格。
- 所見の採用：利用者指示により§4を追記更新（正準位置推定・自由記入節敵対fixtureの反映。
  commit `88d3ceb`）。**自由文類型の想定用途（参照文書の横断整合検査）が初回で実益を出した**。

## 3. 受入条件の充足状況

- §9-1〜4：本record §1（RED→緑・golden byte不変・共通検査の両向き）で充足。
- §9-5：**成立**（e2e-013-001。組み立て→記入→check→commit→起動→転記→照合→所見採用の全経路）。
- §9-6：正規全試験2,482件成功・終了コード0。§6保護対象は変更なし。
- §9-7：未実施（agy・`completion_review`類型。本record作成後に起動）。
- §9-8：未実施（§7.4残余risk 5点の利用者受入判断）。
