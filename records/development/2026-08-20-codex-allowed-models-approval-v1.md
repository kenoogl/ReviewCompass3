# 契約候補015 codex許可model一覧の利用者承認record v1

- 判断日：2026-08-20
- 判断者：利用者（Human）
- 記録者：Claude
- 判断の種別：codex-cli第3 backend契約候補（015）の範囲確定入力2点——(1) 許可model一覧の確定
  （事前走査v1 §8-3の論点。契約010・012の「利用者承認recordで確定」と同型）、(2) 保留候補
  `IC-REQUEST-BUILDER-MODEL-CHECK-SCOPE-001`再評価の扱いの裁定（同§8-5）

## 1. 承認文言【記録】

> 範囲案どおり契約候補v1を作成してください。保留候補の再評価は(b)別作業単位のままにします。
> codexの許可モデルはgpt-5.6-sol, gpt-5.6-terra

（2026-08-20 chat。範囲案＝事前走査record
`records/development/2026-08-20-codex-cli-backend-prescan-v1.md`の提示後にClaudeがchatで示した
契約候補骨子。選択肢(b)＝「本契約後の独立小作業単位に切る」＝同record §8-5の推奨案）

## 2. 確定事項

1. codex-cli backendの許可model一覧を**`("gpt-5.6-sol", "gpt-5.6-terra")`の2値**（利用者の列挙順）
   で確定する。契約015の実装で直書きの契約固定定数として置き、空開始の段階を省略する
   （承認が契約候補作成前に得られたため。契約010・012の型の先行履行）。
2. 起動時のrequested modelは既存実装の規則（一覧の先頭要素）に従い`gpt-5.6-sol`となる。
   `gpt-5.6-terra`は**許可済みだが、起動時に選択する機構は契約015の範囲外**（必要になった時点で
   小改定として扱う）。
3. 和集合`ALLOWED_RESPONSE_MODELS`は末尾追加により
   `("gemini-3.1-pro-high", "claude-opus-5", "gpt-5.6-sol", "gpt-5.6-terra")`となる。先頭要素
   （agy値＝依頼recordの既定記載）は不変であり、契約011対象試験の無変更緑を受入条件で機械確認する。
4. 以後の一覧変更は契約改定として扱う（契約010・012と同じ規律）。
5. `IC-REQUEST-BUILDER-MODEL-CHECK-SCOPE-001`（依頼record model欄とbackendの対応検査）は
   **保留のまま、契約015の範囲外**とする。着手する場合は契約015受入後の独立小作業単位として
   別途定義する（台帳上の候補状態は変更しない）。

## 3. 参考実測【記録】

疎通の実測確認（事前走査record §7・2026-08-20）で、この端末のcodex既定model表示は`gpt-5.6-sol`
だった。承認された先頭値と一致する。stream内のmodel表記の正準位置（JSONL上のどのeventにどの
表記で現れるか）は契約015のRED段実測で確定する。

## 4. 未実施

- 定数の実装（契約015の実装段で行う。本記録時点で製品コードは無変更）。
- 実E2E（契約015 §9の承認付き実測へ）。
