# Session記録安全保存 境界8 確認済み削除TDD Evidence v1

- 実施日：2026-08-15
- 開始基準commit：`ec50cf6`

【実測】誤確認値3例、削除と監査保持1例、片側`deleting`書込み・raw・派生物・確定印の各停止点からの
再試行4例を先に追加した。単独実行は終了コード1、8 failedで、主要理由は`delete_record`不在だった。

【実測】計画の確認値を無変更状態で再照合し、両rootへ同じ確認値を持つ`deleting`を先に同期した後だけ、
記録済みの固定本文と一時fileを順序削除する処理を追加した。各削除直前に記録directoryとfileの所有者、種類、
mode、追加ACL、symlink非追跡open後条件を再確認する。本文不存在を確認してから`deleted.json`を同期・再読込みし、
操作情報を最後に除く。削除済み内容の個別識別値は保持せず、その正準集合のSHA-256だけを監査印へ残す。

【実測】同じ確認値を`deleting`へ保持するため、片側操作情報更新後、raw、派生物、確定印の各故障注入後に
同じ要求を再試行できた。誤確認値では前後のbytes、inode、mtimeが不変だった。監査印は元の保持期限前に
`audit_retention_active`で拒否され、期限後の新しい明示計画と確認値だけで除去できた。他記録directoryは残った。

【実測】単独試験は終了コード0、8 passed。専用試験55 passed、既存入口関連21 passed、
`git diff --check`は終了コード0だった。

- 実装SHA-256：`fc8eb26ae37d0fe6d7891d185964b3afcb03fc6f9ed004c2997ec05b055538c9`
- 試験SHA-256：`1dbcd4f1fea33d9b707529097bb7c8e594aebcac0c078d1448db9f87b4cf24a0`

【判断】境界8はREDを変えずGREENとなった。自動削除、他記録探索、root削除は実装していない。
四操作を一つの配布用実行名から呼ぶ製品入口は境界9まで未実施である。
