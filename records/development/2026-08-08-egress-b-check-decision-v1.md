# B答え合わせ 結果・基準Decision v1

- decision ID：`DEC-EGRESS-B-CHECK-001`
- decision maker：Human
- decided at：2026-08-08
- 対象：A1判定record（SHA-256 `2f8dda0d443666a2512bca5207bd0573f5667b190507224413420e8032ba8ef3`）の59組
- 確定文言：Human「OK.」（2026-08-08。最終形と固定内容の提示後）

## 1. 確定した手順（Human文言は逐語）

> 「このケースは、入れ物だけでなく、今やったように使われ方を調べ、判断を示す。コメントを追記して、
> Humanに提示、判断を仰ぐというのが筋。」

判定が割れる組は、定義の見た目だけで決めない。**使われ方（生成箇所・利用module数・呼び出し行数）を
実測し、所見をコメントとして付してHumanに提示し、判断を仰ぐ。**

## 2. 確定した判断基準

1. **効果基準**：「統合した場合の効果として、コード量が大幅に減る、見通しが良くなるなどの
   メリットがあるかどうかが判断のポイント」（Human 2026-08-08）
2. **統一定義（そのままでの置き換え可能性のみで決める）**：
   - same＝双方向に置き換え可能（違いは名前・整形だけ）
   - partial＝**ちょうど片方向だけ**置き換え可能（入れ物クラスなら項目の包含。
     「入れ物クラスでのpartialはどちらかが他方を包含している場合に限定される」Human 2026-08-08）
   - different＝どちらの向きにも置き換え不可。「手直しすれば共通化できる」は評価②（統合設計）の
     話であり、評価①の判定に混ぜない（#54〜56の裁定「私だとdifferentにする」より）
3. **役割・契約の観点（#10の教訓）**：「入れ物クラスの場合でも単に型だけで判断してはいけない」
   （Human 2026-08-08）。項目が1対1対応でも、名前や役割が外部互換などの契約である場合は
   置き換え不可＝different
4. **型違いの扱い（#9の裁定）**：「型を広げれば統合できる」可能性は使われ方に依存する。
   #9は使われ方の実測（同一file内3生成箇所・外部利用ゼロ・型の違いが保証を表現）と効果基準から
   **統合対象でない**と裁定

## 3. 最終判定（全59組）

**same 48／partial 0／different 11**

- different 11組：#1（Continuity↔CurrentState）、#6（ReviewExecution↔ReviewTriage）、
  #7（StageOneAudit↔StageTwoAudit）、#8（StageOneAudit↔StageGateResult）、
  #9（Obligation↔RequirementSourceRecord）、#10（DeploymentPaths↔_BuiltinPlatformDirs）、
  #11（ToolCall↔ToolResult）、#12（RawReviewRecord↔ParsedReview）、
  #54〜56（`_absolute_path`×3。例外型が違い、そのままではどちらの向きにも置き換え不可）
- same 48組：A1のsame 49組から#10を除いたもの。#2（Policy↔Universe）はsameだが
  統合対象でない（効果小・型の区別という保証が減る）

## 4. A1との一致度と原因分析

- 一致：50／59組（**約85%**）。不一致9組（#7・#8・#9・#10・#11・#12・#54〜56）
- **不一致はすべて「判定基準の未定義」が原因**。A1の判定者には「partial＝中核が同じで無視できない
  差」という緩い定義しか渡していなかった。§2の統一定義を審査基準に明記すれば、A2や再実行では
  解消する見込み。判定者の読み間違い（コードの誤読）は0件だった

## 5. 統合候補（評価②への持ち越し）

統合の本命は精査組ではなく、**same 48組が束なる逐語重複の系統**である：

- SHA-256計算関数×7file（14組）、fail-closed例外クラス×7file（21組）、
  `_within`パス判定×4file、JSON印字×3file、canonical digest計算×4file
- これらはコード減・見通し向上の効果が明確。統合の実施判断（評価②：文脈依存）は別途Humanが行う

## 6. 境界

- 本recordは評価①の判定と基準の固定であり、統合の実施を決めない
- A2（外部API比較）の要否は本recordの結果を材料にHumanが別途判断する
