# 第3段 混在三群の履歴固定候補抽出 Evidence v1

- 記録日：2026-08-13
- 状態：`extracted_pending_independent_review`
- 対象Issue：`ISSUE-TEST-GROWTH-STATE-PINNING-001`
- 作業票：`docs/development/2026-08-13-stage3-mixed-groups-history-pin-extraction-bootstrap-work-ticket-v1.md`
- 作業票SHA-256：`6b04e1597918446a2f07a080266be8cc0c40184fd59b5e8a120af86a0f7c7be7`
- 基準commit：`46d3798`

## 1. 開始確認

【判断】開始可。既存401件一覧からG06、G07、G11の条件で抽出した母集団は24件、8件、78件で、合計110件、
重複0件だった。作業は読み取りと本記録一件に限られ、削除、実装、試験実行、外部確認を含まない。

## 2. 抽出方法と結果

【実測】110件の試験本文をPython構文木で読み、固定commit、Git物体、内容識別値、既存文書・記録、旧形式、
実装範囲、入口、案内文書を確認する名称と条件を持つ13 node IDを第一候補として機械抽出した。残り97件は、
一時ディレクトリの入力を使って現在の製品処理、検査器、保存、状態遷移、外部処理制限を直接実行するため、
履歴固定の軽量候補から除外した。

| 群 | 母集団 | 第一候補 | 軽い読み取り後の履歴専用候補 |
| --- | ---: | ---: | ---: |
| G06 共通内容識別値・経路・出力 | 24 | 4 | 0 |
| G07 赤試験宣言契約 | 8 | 1 | 0 |
| G11 Pilot協働 | 78 | 8 | 0 |
| **合計** | **110** | **13** | **0** |

【判断】固定値、旧形式、日付入り資料、静的な実装範囲を扱うだけでは履歴専用と裁定しない。現在の互換性、
安全、入口の一意性を直接守る場合は現在保証として除外する。

## 3. G06の4件

【実測】`test_known_documents_keep_their_digest`の引数違い3件は、JSON互換な文書について、現在の共通
内容識別値処理が独立した標準JSON＋SHA-256計算と一致することを確認する。過去の特定fileや固定commitを読まない。

【実測】`test_real_ledger_records_keep_their_digest`は、現在の`records/development/*.json`から実在する
`content_digest`付き記録を最大200件読み、現在の共通処理で宣言値と一致することを確認する。

【判断】四件は履歴文言固定ではなく、共通処理変更で現在の文書・台帳互換性を壊さない保証である。G04の
履歴固定候補と結合せず、現役集合へ残す。G06の他20件もJSON不正、経路境界、共有処理結線を直接確認するため、
今回の整理候補から外す。

## 4. G07の1件

【実測】`test_legacy_map_remains_readable_but_cannot_satisfy_version_two`は、旧版1の宣言対応表を現在の検査器が
読める一方、現行条件として最低版2を指定すると拒否することを一時入力で確認する。

【判断】過去の対応表そのものを固定する試験ではなく、現在の後方互換性と合否境界を確認する。G07の他7件も、
欠落、型不正、経路逸脱、失敗理由、収集errorを現在の検査器へ直接投入するため、全8件を整理候補から外す。

## 5. G11の8件

【実測】次の五件は現在の製品コードまたは状態を直接確認する。

- 要求26件と現在の対応表の完全一致
- 現在指示が固定commit後に変わった場合のstale判定
- Pilotコードの外部処理が引数配列のGitだけであること
- PilotのGit処理が読取り命令だけであること
- 閉じた旧レビュー処理をPilotが再解釈しないこと

【実測】入口三件は、現在の`pyproject.toml`の共通入口一つ、`AGENTS.md`と`CLAUDE.md`から共通案内への
各一参照、共通案内が現在の正本指示と三命令、LLM・機械処理・外部送信・Humanの境界を示すことを確認する。
正本指示は日付入りの記録だが、現在の案内文書から直接参照される現役正本である。

【判断】八件は履歴専用ではない。静的検査の能力や保守負担は別途見直し得るが、今回の「履歴だけを固定する
同型候補」には含めない。G11の他70件は、準備、取込、保存、状態確認、入力改変、外部処理制限を現在の製品処理へ
直接確認するため、今回の整理候補から外す。

## 6. G04との結合と実施計画の境界

【判断】G06、G07、G11から、G04の履歴固定候補と同じ実施単位へ追加する試験は0件である。候補0件という結果を、
削減件数を増やすために広げない。

【判断】複数群をまとめた実施計画は次の境界で作る。

1. G04の五単位を対象に、維持、混在分離、履歴固定整理、未使用処理との結合整理を比較する。
2. G06の24件、G07の8件、G11の78件は、今回の実施では現役集合に残すと明記する。
3. G06、G07、G11を将来整理する場合は、履歴固定ではなく、検査能力、重複、共有境界という別目的の作業にする。
4. 削除判断済みのG04一件を含む具体的な変更path、関連試験、戻し方を計画し、その時点で第3段一回目の
   Claude手動確認へ渡す。

## 7. 反証

【実測】中心判断「混在三群にもG04と同型の履歴専用試験がある」ことを支持する候補を13件まで広めに抽出した。
しかし、各候補は現在の共通処理互換性、検査器の版境界、製品の安全、要求対応、現役入口を直接確認していた。
したがって、同型候補があるという反証は成立せず、履歴専用候補0件となった。

【実測】反対に「日付入りの正本指示を読むG11入口試験は履歴専用ではないか」を調べた。現在の共通案内が同指示を
正本として直接参照し、`AGENTS.md`と`CLAUDE.md`が共通案内へ接続するため、現在保証であることを確認した。

## 8. 未実施

【未実施】試験、製品コード、設定、証跡、対応表、宣言データの変更、削除、統合、試験実行、全試験、変異検査、
新しい台帳・検査器・試験の追加、他の13群の調査、実施計画の作成・承認、Claudeへの手動受け渡し、外部送信、
push、履歴書換え、第3段完了は行っていない。
