# 第3段 混在三群の履歴固定候補抽出 独立完了レビュー v1

- レビュー日：2026-08-13
- 状態：`correction_required`
- 観測commit：`5612532b3c8784323443481bd8d74f4f01d69ef2`
- 作業票：`docs/development/2026-08-13-stage3-mixed-groups-history-pin-extraction-bootstrap-work-ticket-v1.md`
- 作業票SHA-256：`6b04e1597918446a2f07a080266be8cc0c40184fd59b5e8a120af86a0f7c7be7`
- 対象成果：`records/development/2026-08-13-stage3-mixed-groups-history-pin-extraction-evidence-v1.md`
- 対象成果SHA-256：`408079c8f9e2834bc5d3cd463f4fc77ab909247a16960f38dd948cf927dc6313`
- レビュー担当：作業担当とは異なる新規サブエージェント

## 1. 判定

【判断】`correction_required`。母集団の件数は一致し、成果が選んだ13件について現在保証とした判断も支持される。
しかし、G11の除外側に、実装時点の変更範囲を試験内の処理だけで確認する三件が残っていた。第一候補13件の
完全性と履歴専用候補0件は確認できないため、完了とは判定しない。

## 2. 固定対象と独立集計

【実測】観測commitは実在し、作業票と対象成果のSHA-256は申告値と一致した。401件一覧へ意味群分類Evidenceの
ファイル条件を独立に適用した結果は、G06が24件、G07が8件、G11が78件、合計110件、重複0件だった。

【実測】試験の構文木から、現在の内容識別値互換性、旧版と現行版の境界、要求対応、入力の古さ、製品コードの
外部処理制限、入口と案内文書を読む条件を再選択すると、成果と同じG06の4件、G07の1件、G11の8件、合計13件に
なった。13件は110件の部分集合で重複0件だった。ただし、これは成果の抽出結果を再現できたことを示すだけで、
除外側に同型候補がないことまでは示さない。

## 3. 13件の現在利用者と保証

【実測】G06の4件のうち、引数違い三件は現在の`tools.common.digests.canonical_content_digest`を、試験側で
独立計算した標準JSONのSHA-256と比較する。残る一件は現在の`records/development/*.json`を最大200件読み、
宣言された内容識別値と現在の共通処理を比較する。観測commitでは同じ共通処理を製品コード11ファイルが直接
取り込んでいた。固定値を使う現在互換性試験を履歴専用へ誤分類していない。

【実測】G07の一件は一時入力を現在の`check_declaration_red_map`へ二回渡し、最低版を指定しない場合は旧版1を
読めること、最低版2を指定した場合は拒否することを同時に確かめる。過去の実ファイルを固定する試験ではなく、
現在の検査器が持つ後方互換性と合否境界の保証である。

【実測】G11の8件は次の現在対象へ結び付く。

- 要求26件と現在の試験名の対応
- 現在のPilot処理による入力の古さ判定
- 現在のPilot製品コード二件の外部処理方法とGit読取り命令
- 現在のPilot製品コードが閉じた旧処理を呼ばないこと
- 現在の`pyproject.toml`、`AGENTS.md`、`CLAUDE.md`、共通案内、共通案内が参照する現役指示

【判断】この13件をG04同型の履歴専用候補から外した判断は支持される。

## 4. 止める指摘

【実測】G11の次の三件は、現在の製品処理を直接実行していない。

- `test_change_scope_ignores_later_record_and_todo_commits`
- `test_change_scope_rejects_forbidden_commit_before_later_allowed_commit`
- `test_change_scope_does_not_hide_code_inside_handoff_directory`

三件が呼ぶ`_implementation_paths_since_base`、`_is_followup_record`、`ALLOWED_PATHS`は
`tests/test_pilot_collaboration_entrypoints.py`内だけにあり、観測commitの製品コードには同じ処理も利用箇所も
存在しない。三件は一時Gitリポジトリへ試験データを作り、試験内の変更範囲計算だけを確かめる。

【実測】追加履歴では、当初の`df48bba`に、固定基準commitから現在の作業場所の変更pathを直接確認する
`test_change_scope_contains_only_v6_allowlisted_paths`があった。後続の`f2e4be9`、`7816df1`、`6cb26e7`で
試験内の計算処理と三つの一時入力例へ展開され、`354c57e`で現在の作業場所へ適用する元の一件だけが削除された。

【判断】三件は少なくとも「実装時点の範囲を確認する第一候補」に含め、現在利用者、現在保証との重複、
固定commitからの回復可能性を軽く分類する必要がある。これを行わず、第一候補13件と履歴専用候補0件を確定しては
ならない。削除可否は本レビューでは裁定しない。

## 5. 報告不一致

【実測】対象成果§2の「残り97件は現在の製品処理、検査器、保存、状態遷移、外部処理制限を直接実行する」と、
§5の「G11の他70件は現在の製品処理へ直接確認する」は、上記三件には当てはまらない。報告不一致は一系統、
影響する表示は第一候補13件、履歴専用候補0件、G11の除外理由、実施計画の境界である。

## 6. 試した反証

1. 【実測】固定値を持つG06四件を履歴専用と仮定したが、現在の共通処理と独立計算・現在台帳を直接比較し、
   製品コード11ファイルの利用もあったため反証は不成立だった。
2. 【実測】旧版を使うG07一件を過去資料固定と仮定したが、現在の検査器へ一時入力を渡して現行最低版との境界を
   確認していたため反証は不成立だった。
3. 【実測】G11の除外70件にG04同型がないという中心判断を、現在製品への呼出しと追加履歴から反証した。
   上記三件は試験内だけの処理へ接続し、現在の作業場所へ適用する元試験が削除済みだったため反証は成立した。
4. 【実測】110件へ広めの静的信号検索を行い、内容識別値、現在リポジトリ読取り、文書path、Git物体語を持つ
   27件を確認した。第一候補以外の信号を本文で軽く分けた結果、G06の異常入力群は現在の共通処理、G11の上記
   三件は試験内だけの変更範囲処理へ結び付いた。この差が見落としの所在を再現した。

## 7. 未実施

【未実施】対象成果、作業票、試験、製品コード、設定、証跡、対応表、宣言データの変更、110件全件の必要性判断、
削除案、試験実行、全試験、変異検査、他群、新機構、Claude確認、外部送信、push、履歴書換え、第3段完了判断は
行っていない。

## 8. 次の一作業

【提案】対象成果v1を履歴として残し、上記三件だけを第一候補へ追加して役割を軽く分類した限定修正版v2を作る。
G06、G07、既確認のG11八件、残りの試験、削除判断、実施計画へは広げない。
