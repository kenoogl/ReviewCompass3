# TODO_NEXT_SESSION

更新日：2026-07-27
対象：ReviewCompass3

## 現在地

- `main` は `origin/main` より先行しており、未pushのコミットがある
- 本TODO更新前の最新機能コミットは
  `9075bb2 Verify preserved raw logs with integrity ledger`
- 最小リポジトリ骨格を作成済み
- ReviewCompass と ReviewCompass2 の参照コミット、ファイル一覧、照合値を記録済み
- セッションログ基盤の移植候補と既存テストを棚卸し済み
- セッションログ基盤の暫定的な個別責務をTDDで実装済み
- 要約の統括パイプライン・保存接続、設定・CLI入口、原子的保存と
  障害復旧、機微情報報告と許可リスト、転写再生成比較をTDDで実装済み
- CLI運用強化、保存排他、ライフサイクルフック、生ログ保全・復元、
  再生成条件診断をTDDで実装済み
- 保全の設定・CLI接続、再生成診断CLI、Claude Codeフック導入・解除、
  定期保全入口、Codex公開JSONL解析をTDDで実装済み
- Codex追加item解析、固定フックコマンド、保全一覧・復元CLI、
  所有権付き共通ロック、構造化CLI結果をTDDで実装済み
- 実データ形状E2E、独立変異検査、フック観測記録、定期保全plist、
  Git保存境界をTDDで実装済み
- 要約再生成、cwd非依存入口、launchd状態管理、私的検証ハーネス、
  保全完全性台帳をTDDで実装済み
- 全テスト結果：`86 passed`
- 作業ツリーは本TODO更新前の時点でクリーン
- intent、requirements、design は未着手

## 次回セッションの目的

再構築計画の「第0段：新リポジトリとセッションログ基盤」を継続する。
主要な取り込み・安全化・保存・照合処理が接続されたため、
次はCLI処理で要約材料を安全に収集・固定する作業を第一候補とする。
期待する入出力と失敗時に残す情報を明文化してから、テストと実装を分割した
次のTDDサイクルを進める。

## 完了済み

- 最小リポジトリ骨格
- 暫定成果物の lifecycle 表示
- 参照状態の固定と照合値記録
- セッションログ基盤の移植候補・既存テストの棚卸し
- 生セッションログの再帰的発見と探索ルート検証
- シンボリックリンクの対象外化
- Claudeログ種別の識別
- Claudeの利用者・アシスタント・道具呼出・道具結果のイベント解析
- 壊れた行、未対応イベント、不完全イベントの診断
- 決定的な最小Markdown転写
- 元ログ、取り込み範囲、照合値、ツール版の来歴
- 設定された既知パターンの伏字化
- イベント列の冪等な追記専用更新と非追記変更の検知
- 識別、解析、転写、伏字化、来歴生成を接続する最小統括パイプライン
- 高エントロピー候補の検出と残存時fail-closed
- 来歴に記録したraw範囲と転写の独立照合
- 転写と来歴JSONの配置、冪等保存、末尾追記、非追記変更時の保全
- 利用者発言、コミット、変更ファイルを列挙する伏字化済み要約
- 要約の統括パイプライン接続、来歴digest記録、転写・要約・来歴の一括保存
- 設定ファイルからの各ルート、伏字化規則、ツール版の読み込み
- 発見、解析、安全化、保存を接続するCLI入口
- 一時ファイルと置換を使った成果物の原子的更新、失敗時の既存成果物復元
- 高エントロピー候補の安全なJSON報告と完全一致の許可リスト
- 機微情報残存時の成果物保存中止とCLI終了値`2`
- 来歴の記録範囲からの転写再生成
- 生ログ、来歴digest、保存済み転写の独立した一致判定
- CLIのdry-run、対象なし・未対応形式・一般障害を区別する終了値
- 成果物単位の排他、使用中検知、期限切れロック回収
- 無効時に通常作業を妨げない開始時・終了時フック入口
- 生ログの追記専用保全、非追記変更の保全、消失時の復元
- 伏字化規則・許可リストdigestの来歴記録
- 生ログ・規則・ツール版・転写変更を区別する再生成診断
- 再生成不能時に値を含めない安全な診断
- 設定・通常CLI・終了フックからの生ログ保全
- `--verify`による安全なJSON Lines再生成診断
- Claude Code設定への既存値を保持した冪等なフック導入・解除
- 保全ファイル単位の排他と`--preserve-only`定期実行入口
- Codex公開`codex exec --json`形式の識別、メッセージ・コマンド解析
- Codex公開JSONLから保存した転写の再生成照合
- Codex公開JSONLのMCP呼出、ファイル変更、Web検索、計画更新の解析
- 開始・終了phaseを検証する固定フックコマンド入口
- 保全済みログの一覧表示と明示相対パスからの安全な復元CLI
- 所有権トークン付き共通ロックと所有者以外による解除の防止
- 通常処理、dry-run、保全、検証の共通JSON Lines結果と件数集約
- ClaudeとCodexの値を除いた実データ形状fixtureによる保存・照合・復元E2E
- LF以外の行区切り、digest、伏字化、非追記変更の独立変異検査
- セッションを妨げない開始・終了フック結果の安全なJSONL観測記録
- 定期保全用LaunchAgent plistの安全な生成、冪等導入・解除
- 明示したリポジトリ境界に基づく非Git・Git成果物の配置強制
- rawからの要約再生成、来歴digest・保存済み要約との独立照合
- リポジトリ外cwdから実行できるフック・定期保全の絶対入口
- 所有plistだけを対象とするlaunchd起動・停止・状態管理境界
- リポジトリ外の明示ログを件数だけで検証する私的検証ハーネス
- 保全ログのサイズ・SHA-256台帳と改変時の復元拒否

## TDD分割コミット

| 責務 | テストコミット | 実装コミット |
|---|---|---|
| 生ログ発見 | `17970c2` | `226464a` |
| Claudeログ識別 | `d4026ce` | `d3dbaa5` |
| 探索ルート検証 | `83bca7a` | `be2478a` |
| シンボリックリンク除外 | `51ac778` | `3bfae39` |
| Claude最小イベント解析 | `3602370` | `4cee96c` |
| 解析診断 | `a4a7963` | `1d94287` |
| 読取失敗・不完全イベント | `accd088` | `ab2fb9f` |
| 道具呼出・結果の解析 | `92f7c14` | `bb84821` |
| 最小転写 | `1e1b3bd` | `30a387d` |
| 不完全な道具イベント | `304c0d5` | `7e46f6b` |
| 来歴と取り込み範囲 | `db2ecaf` | `31149b4` |
| 既知パターンの伏字化 | `7ac4f1b` | `7b76286` |
| 追記専用更新 | `28e52c7` | `afbec66` |
| 最小統括パイプライン | `4736853` | `75929ab` |
| 高エントロピーfail-closed | `7d0a12f` | `78723f9` |
| 来歴照合・改変検知 | `2a80e64` | `32af12a` |
| 出力配置・実ファイル保存 | `4d4a836` | `1d88c6d` |
| 人が読む要約 | `59c0d7a` | `9888853` |
| 要約のパイプライン・保存接続 | `6ff91cc` | `009f077` |
| 設定とCLI入口 | `1322145` | `01e0086` |
| 保存の原子的更新と障害復旧 | `70ebdd2` | `af0a75f` |
| 機微情報報告と許可リスト | `381a6be` | `13eac2e` |
| 転写の再生成比較 | `897f8d2` | `b3023bb` |
| CLIのdry-runと終了値 | `f6e45e8` | `57c84de` |
| 保存処理の排他制御 | `0a8d622` | `c69f5c2` |
| 開始時・終了時フック | `096a516` | `8cbf7a5` |
| 生ログの別領域保全と復元 | `1921b3c` | `ea61f60` |
| 診断情報と再生成条件 | `8e08403` | `017ceae` |
| 保全の設定・CLI・フック接続 | `dbcb7fe` | `32f111b` |
| 再生成診断のCLI接続 | `02437fd` | `477a355` |
| Claude Codeフック導入・解除 | `3aed74e` | `b0badba` |
| 保全排他と定期実行入口 | `cfde647` | `b14828f` |
| Codex公開JSONLの識別・解析 | `c94e5dc` | `75ac79e` |
| Codex転写の再生成 | `8f3ef03` | `a9b9bc2` |
| Codex追加item解析 | `c24f059` | `7cf31cc` |
| 固定フックコマンド入口 | `6700c3e` | `cf6159c` |
| 保全一覧・復元CLI | `07c9e9c` | `94ed840` |
| 所有権付き共通ロック | `f7c9284` | `ee2174d` |
| CLI結果の構造化 | `658e80b` | `5443ade` |
| 実データ形状fixtureのE2E | `57b60cc` | `6340a95` |
| 独立変異検査 | `e3ee9ce` | `c7c7f99` |
| フック失敗の観測記録 | `095ac2b` | `38ca415` |
| 定期保全LaunchAgent設定 | `c2b1775` | `39c0550` |
| Git保存境界 | `99f6989` | `327a83f` |
| 要約の再生成と改変検知 | `5b024c2` | `3f2f19a` |
| cwd非依存の固定入口 | `c30f31a` | `7d010f3` |
| launchd起動・状態管理 | `774c5aa` | `9441cc6` |
| 私的ログ検証ハーネス | `1f8b713` | `b5dc67f` |
| 保全ログ完全性台帳 | `09cb35f` | `9075bb2` |

## 現在の実装境界

- Codexは公開`codex exec --json`形式だけを対象とし、不安定な内部
  `$CODEX_HOME/sessions`形式は意図的に対象外
- Codex解析はメッセージ、コマンド実行、MCP、ファイル変更、Web検索、
  計画更新を対象とし、それ以外のitem種別は安全な診断へ残す
- Claude Codeフック導入処理は明示された設定ファイルだけを扱い、
  固定コマンドを生成するが、実ユーザー設定への自動導入やCodexフック導入は未実装
- 生ログ復元CLIは明示相対パスだけを扱い、既存の異なる元ログを上書きしない
- 成果物保存と生ログ保全は所有権付き共通ロックを使用する
- `--json-lines`は通常処理、dry-run、保全、検証の結果と件数を値なしで出力する
- 安全なfixtureでE2E検証しているが、利用者の実ログを使った私的領域での
  実データ検証は未実施
- 変異検査はLF行境界、digest、伏字化、追記判定の4種類を対象とする
- フック観測記録は明示された非Gitパスだけへ書き、記録自体の失敗も
  セッション継続を優先して伝播させない
- LaunchAgent plistはリポジトリ外cwdから実行でき、`launchctl`の
  起動・停止・状態管理APIもあるが、実ユーザー環境では未実行
- Git保存境界は`repository_root`を明示した設定だけで強制し、
  未指定の既存設定は互換性のため従来どおり扱う
- 転写と要約の再生成照合は実装済みだが、通常CLIはコミット・変更ファイルを
  要約材料としてまだ収集していない
- 私的検証ハーネスはAPIまでで、固定コマンド入口には未接続
- 保全完全性台帳は設定時だけ有効で、既存の台帳なし設定は互換性のため
  従来どおり復元する

## 次の作業候補

候補は採用順ではない。次回は1つを選び、複数の責務を同時に実装しない。

### A. CLI要約材料の安全な収集

- リポジトリのコミットと変更ファイルを明示した範囲から収集する
- 値を伏字化して要約と来歴へ同じ材料を渡す
- 再生成時にも同じ固定材料だけを使用する

### B. フック観測設定の導入接続

- 非Git観測ログパスを設定から固定コマンドへ接続する
- フック導入処理が観測付きコマンドを生成することを保証する
- 観測ログ自体の障害を安全な標準エラーへ縮約する

### C. 定期保全管理の固定CLI

- plist生成、導入、起動、状態確認、停止、解除を単一入口へ接続する
- 標準出力・標準エラーの親ディレクトリを安全に準備する
- 実環境変更前にdry-runで対象と操作を確認できるようにする

### D. 私的ログ検証の固定CLI

- raw、リポジトリ、証拠の絶対パスを明示指定する
- 実行結果を件数とGit前後一致だけでJSON出力する
- 利用者が指定していないログを自動探索しない

### E. 第0段完了関門の機械監査

- 計画5.5の各関門を証拠パスと検査結果へ対応づける
- 未実施の実ログ検証と実環境運用確認を未処理として残す
- 未処理がある限り第0段完了をfail-closedで拒否する

### F. 外部確認後の責務

- 利用者が明示した実ログで私的検証を実行する
- 明示承認後に実ユーザー環境のフックと定期保全を確認する
- 利用者承認後にだけ第1段へ進む
- intent、requirements、designは第1段開始まで作成しない

## 次のTDDサイクル

1. 再構築計画、棚卸し、本 TODO を確認する。
2. 上記候補から責務を1つ選ぶ。
3. 期待する入出力と対象外を明文化する。
4. 実装コードを書かず、テストだけを作成する。
5. テストを実行し、意図した理由で失敗することを確認する。
6. テストが正しいことを確認し、テストだけをコミットする。
7. テストを変更せず実装コードを修正する。
8. 全テストの通過を確認し、実装だけをコミットする。

## 完了条件

- 次の責務について、期待する入出力と対象外が明文化されている。
- テストが意図した理由で失敗することを確認している。
- テストだけのコミットと実装だけのコミットが分かれている。
- 既存テストを変更せず、追加した実装で全テストが通過している。

## 制約

- intent、requirements、design の作成を開始しない。
- ReviewCompass と ReviewCompass2 は読み取り専用とし、ファイルを一括コピーしない。
- ブートストラップ成果物には次を明記する。

  ```yaml
  lifecycle: provisional
  normative_status: non-normative
  promotion_required: true
  ```

- 各TDDサイクルで、テストだけのコミットが完了するまで実装コードを書かない。
- 各TDDサイクルの実装中はテストを変更せず、実装コードを修正する。
- インデントは常に2スペースとする。

## 次回開始時の確認コマンド

```sh
pwd
git status --short --branch
git log -10 --oneline --decorate
python3 -m pytest
```
