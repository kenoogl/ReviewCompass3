# TODO_NEXT_SESSION

更新日：2026-07-27
対象：ReviewCompass3

## 現在地

- `main` は `origin/main` より先行しており、未pushのコミットがある
- 本TODO更新前の最新機能コミットは
  `afbec66 Add idempotent append-only session updates`
- 最小リポジトリ骨格を作成済み
- ReviewCompass と ReviewCompass2 の参照コミット、ファイル一覧、照合値を記録済み
- セッションログ基盤の移植候補と既存テストを棚卸し済み
- セッションログ基盤の暫定的な個別責務をTDDで実装済み
- 全テスト結果：`19 passed`
- 作業ツリーは本TODO更新前の時点でクリーン
- intent、requirements、design は未着手

## 次回セッションの目的

再構築計画の「第0段：新リポジトリとセッションログ基盤」を継続する。
現在は個別責務が分離して存在するため、次は最小統括パイプラインを第一候補とする。
期待する入出力と失敗時に残す情報を明文化してから、テストと実装を分割した
次のTDDサイクルを進める。

## 完了済み

- 最小リポジトリ骨格
- 暫定成果物の lifecycle 表示
- 参照状態の固定と照合値記録
- セッションログ基盤の移植候補・既存テストの棚卸し
- 生セッションログの再帰的発見と探索ルート検証
- シンボリックリンクの対象外化
- Claudeログ種別の識別
- Claudeの利用者・アシスタント・道具呼出・道具結果のイベント解析
- 壊れた行、未対応イベント、不完全イベントの診断
- 決定的な最小Markdown転写
- 元ログ、取り込み範囲、照合値、ツール版の来歴
- 設定された既知パターンの伏字化
- イベント列の冪等な追記専用更新と非追記変更の検知

## TDD分割コミット

| 責務 | テストコミット | 実装コミット |
|---|---|---|
| 生ログ発見 | `17970c2` | `226464a` |
| Claudeログ識別 | `d4026ce` | `d3dbaa5` |
| 探索ルート検証 | `83bca7a` | `be2478a` |
| シンボリックリンク除外 | `51ac778` | `3bfae39` |
| Claude最小イベント解析 | `3602370` | `4cee96c` |
| 解析診断 | `a4a7963` | `1d94287` |
| 読取失敗・不完全イベント | `accd088` | `ab2fb9f` |
| 道具呼出・結果の解析 | `92f7c14` | `bb84821` |
| 最小転写 | `1e1b3bd` | `30a387d` |
| 不完全な道具イベント | `304c0d5` | `7e46f6b` |
| 来歴と取り込み範囲 | `db2ecaf` | `31149b4` |
| 既知パターンの伏字化 | `7ac4f1b` | `7b76286` |
| 追記専用更新 | `28e52c7` | `afbec66` |

## 現在の実装境界

- ログ種別識別と解析はClaudeだけで、Codexは未実装
- 転写は文字列生成までで、配置・実ファイル保存・要約は未実装
- 伏字化は設定された既知パターンだけで、高エントロピー検出と残存時fail-closedは未実装
- 来歴は生成までで、取り込み範囲からの再生成・照合・改変検知は未実装
- 追記専用更新は純粋なイベント列の判定までで、実ファイルへの反映は未実装
- 開始時・終了時フック、定期保全、生ログの別領域保全・復元は未実装

## 次の作業候補

候補は採用順ではない。次回は1つを選び、複数の責務を同時に実装しない。

### A. 最小統括パイプライン

- 発見、種別識別、解析、転写、伏字化、来歴、更新判定を1本の入口で接続する
- 各段階の診断と停止条件を失わずに返す
- ファイル保存を含めず、まず入力から成果物候補を作るところまでを対象にする

### B. 高エントロピー検出とfail-closed

- 既知パターン以外の高エントロピー候補を検出する
- 伏字化後の残存検査と、Git対象成果物を生成しない停止条件を決める
- 機微情報報告はGit対象外へ分離する

### C. 来歴からの再生成・改変検知

- 記録済みの取り込み範囲を元ログから再取得する
- raw、転写、来歴の照合値を検査する
- 元ログへの追記後も、記録済み範囲だけを照合できるようにする

### D. 出力配置と実ファイル保存

- 転写、要約、来歴、機微情報報告の保存区分を実装する
- 追記専用更新の判定を実ファイルへ安全に反映する
- 非追記変更では既存成果物を上書きせず保全する

### E. 人が読む要約

- 利用者発言、コミット、変更ファイルを決定的に列挙する
- 利用者の決定を機械で推測しない
- Git対象へ置く前に伏字化と残存検査を通す

### F. 後続の責務

- 開始時・終了時フック
- 生ログの別領域保全と復元
- セッション非利用期間の定期保全
- Codex入力形式の識別と解析

## 次のTDDサイクル

1. 再構築計画、棚卸し、本 TODO を確認する。
2. 上記候補から責務を1つ選ぶ。
3. 期待する入出力と対象外を明文化する。
4. 実装コードを書かず、テストだけを作成する。
5. テストを実行し、意図した理由で失敗することを確認する。
6. テストが正しいことを確認し、テストだけをコミットする。
7. テストを変更せず実装コードを修正する。
8. 全テストの通過を確認し、実装だけをコミットする。

## 完了条件

- 次の責務について、期待する入出力と対象外が明文化されている。
- テストが意図した理由で失敗することを確認している。
- テストだけのコミットと実装だけのコミットが分かれている。
- 既存テストを変更せず、追加した実装で全テストが通過している。

## 制約

- intent、requirements、design の作成を開始しない。
- ReviewCompass と ReviewCompass2 は読み取り専用とし、ファイルを一括コピーしない。
- ブートストラップ成果物には次を明記する。

  ```yaml
  lifecycle: provisional
  normative_status: non-normative
  promotion_required: true
  ```

- 各TDDサイクルで、テストだけのコミットが完了するまで実装コードを書かない。
- 各TDDサイクルの実装中はテストを変更せず、実装コードを修正する。
- インデントは常に2スペースとする。

## 次回開始時の確認コマンド

```sh
pwd
git status --short --branch
git log -10 --oneline --decorate
python3 -m pytest
```
