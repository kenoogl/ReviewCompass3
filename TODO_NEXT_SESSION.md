# TODO_NEXT_SESSION

更新日：2026-07-27
対象：ReviewCompass3

## 現在地

- `main` は `origin/main` より9コミット先行
- HEAD は `226464a Implement raw session log discovery`
- 最小リポジトリ骨格を作成済み
- ReviewCompass と ReviewCompass2 の参照コミット、ファイル一覧、照合値を記録済み
- セッションログ基盤の移植候補と既存テストを棚卸し済み
- 生セッションログ発見の最初のTDDサイクルを完了済み
  - テスト：`tests/test_session_log_discovery.py`
  - 実装：`tools/session_logs/discovery.py`
  - テストコミット：`17970c2 Add red test for raw session log discovery`
  - 実装コミット：`226464a Implement raw session log discovery`
  - 確認結果：`1 passed`
- `discover_raw_logs` は、指定したルート以下の `.jsonl` ファイルを再帰的に発見し、
  ソート済みの相対POSIXパスのタプルを返す

## 次回セッションの目的

再構築計画の「第0段：新リポジトリとセッションログ基盤」を継続する。
下記の作業候補から次の責務を1つ選び、期待する入出力を明文化してから、
テストと実装を分割した次のTDDサイクルを進める。

## 完了済み

- 最小リポジトリ骨格
- 暫定成果物の lifecycle 表示
- 参照状態の固定と照合値記録
- セッションログ基盤の移植候補・既存テストの棚卸し
- 生セッションログの再帰的発見

## 次の作業候補

候補は採用順ではない。次回は1つを選び、複数の責務を同時に実装しない。

### A. 生ログ発見の境界条件

- 空ディレクトリ、存在しないルート、対象外ファイルなどの契約を決める
- シンボリックリンクや読み取り不能ファイルを扱う範囲を決める
- 現在の最小実装に追加すべき振る舞いだけをテストで固定する

### B. 入力形式とログ種別の識別

- Claude、Codexなどの入力形式を、ファイル発見とは別責務で識別する
- 識別できない入力の結果を明文化する
- まず1種類の最小fixtureと判定結果から始める

### C. rawログの最小イベント解析

- 1つの入力形式について、利用者発言とアシスタント発言を正規化したイベントへ変換する
- 壊れた行、空行、未対応イベントをどう扱うか決める
- 転写や要約は含めず、解析境界だけを対象にする

### D. rawログからの最小転写

- 正規化済みイベントから、人が追える決定的な転写を生成する
- 思考、道具呼び出し、道具結果の扱いは契約を決めてから段階的に追加する
- 要約、伏字化、保存は別のTDD単位とする

### E. 来歴と取り込み範囲の記録

- 元ログの相対パス、取り込み範囲、SHA-256、ツール版を記録する
- 元ログへの追記後も、取り込み済み範囲を照合できる契約を決める
- 再生成と改変検知は、来歴形式を固定した後の別単位とする

### F. 後続の責務

- 機微情報の伏字化と残存時のfail-closed
- 冪等な追記専用更新と変更検知
- 人が読む要約
- 出力配置と保存区分
- 開始時・終了時フック
- 生ログの別領域保全と復元
- セッション非利用期間の定期保全

## 次のTDDサイクル

1. 再構築計画、棚卸し、本 TODO を確認する。
2. 上記候補から責務を1つ選ぶ。
3. 期待する入出力と対象外を明文化する。
4. 実装コードを書かず、テストだけを作成する。
5. テストを実行し、意図した理由で失敗することを確認する。
6. テストが正しいことを確認し、テストだけをコミットする。
7. テストを変更せず実装コードを修正する。
8. 全テストの通過を確認し、実装だけをコミットする。

## 完了条件

- 次の責務について、期待する入出力と対象外が明文化されている。
- テストが意図した理由で失敗することを確認している。
- テストだけのコミットと実装だけのコミットが分かれている。
- 既存テストを変更せず、追加した実装で全テストが通過している。

## 制約

- intent、requirements、design の作成を開始しない。
- ReviewCompass と ReviewCompass2 は読み取り専用とし、ファイルを一括コピーしない。
- ブートストラップ成果物には次を明記する。

  ```yaml
  lifecycle: provisional
  normative_status: non-normative
  promotion_required: true
  ```

- 各TDDサイクルで、テストだけのコミットが完了するまで実装コードを書かない。
- 各TDDサイクルの実装中はテストを変更せず、実装コードを修正する。
- インデントは常に2スペースとする。

## 次回開始時の確認コマンド

```sh
pwd
git status --short --branch
git log -10 --oneline --decorate
python3 -m pytest
```
