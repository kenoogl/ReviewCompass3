# TODO_NEXT_SESSION

更新日：2026-07-27
対象：ReviewCompass3

## 現在地

- Git リポジトリ作成済み
- `README.md` の初回コミット済み（`d4142c3`）
- 再構築計画を `docs/plan/2026-07-27-reviewcompass3-rebuild-plan.md` に作成済み
- 本 TODO を作成済み
- 実装・テストは未着手

## 次回セッションの目的

再構築計画の「第0段：新リポジトリとセッションログ基盤」を開始し、最初の赤テストを
テストだけでコミットできる状態まで進める。

## 実施順序

1. 再構築計画と本 TODO の内容を確認する。
2. 次の最小リポジトリ骨格を作る。
   - `AGENTS.md`
   - `pyproject.toml`
   - `tests/`
   - `tools/`
   - `config/`
   - 記録用の最小ディレクトリ
   - 配布物とアプリ側成果物を区別する最小 manifest
3. ReviewCompass と ReviewCompass2 の参照コミットを固定し、ファイル一覧と照合値を
   調査記録へ残す。
4. セッションログ基盤の移植候補と既存テストを棚卸しする。
5. 棚卸し結果から最初の小さな実装単位を決め、期待する入出力を明文化する。
6. 実装コードを書かず、その入出力に対するテストだけを作成する。
7. テストを実行し、意図した理由で失敗することを確認する。
8. テストが正しいことを確認し、テストだけをコミットする。

## 完了条件

- 最小リポジトリ骨格が存在する。
- 参照コミット、ファイル一覧、照合値が記録されている。
- セッションログ基盤の移植候補と既存テストの棚卸し結果が残っている。
- 最初のテストが実装不在を理由に失敗する。
- 実装コードを含めず、テストだけのコミットが作成されている。

## 制約

- intent、requirements、design の作成を開始しない。
- ReviewCompass と ReviewCompass2 は読み取り専用とし、ファイルを一括コピーしない。
- ブートストラップ成果物には次を明記する。

  ```yaml
  lifecycle: provisional
  normative_status: non-normative
  promotion_required: true
  ```

- テストだけのコミットが完了するまで実装コードを書かない。
- 実装中はテストを変更せず、実装コードを修正する。
- インデントは常に2スペースとする。

## 次回開始時の確認コマンド

```sh
git status --short
git log -5 --oneline --decorate
```
